#ifndef CLAWDMETER_C_WEBKITGTK_SHIM_H
#define CLAWDMETER_C_WEBKITGTK_SHIM_H

// WebKitGTK 6.0 — GTK 4 build of WebKit. Used by InAppBrowser for the
// Cmd-click element-comment overlay. Bundle bubblewrap + xdg-dbus-proxy
// + GStreamer plugins in AppImage (codex C9).
// pkg-config: webkitgtk-6.0
#include <webkit/webkit.h>

#endif
