#ifndef CLAWDMETER_C_CAIRO_SHIM_H
#define CLAWDMETER_C_CAIRO_SHIM_H

// Cairo 2D drawing library.
// pkg-config: cairo (resolves -I/usr/include/cairo on most distros).
// Used for: live menu-bar gauge PNG rendering (CairoGaugeRenderer),
// stacked bar chart in Dashboard (CairoBarChart), popover sparkline.
#include <cairo/cairo.h>

#endif
