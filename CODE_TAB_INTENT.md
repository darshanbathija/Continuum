# Continuum Code Tab Intent Map

Date: 2026-06-09

Latest source refresh: 2026-06-10 01:30 ICT, on clean `main` at `d215a7c5` (PR #286 merged).

This is the phase-one source-of-truth spec for the primary `Code` tab. It is derived from the most recent 150 merged PRs that touched Code-tab UI, Code-tab runtime plumbing, provider/model routing, or Code-tab tests. It excludes Settings/Usage/menu-bar behavior except where a Code-tab control intentionally opens or depends on those surfaces.

Source PR window: refreshed from GitHub with `gh pr list --repo darshanbathija/Continuum --state merged --limit 300 --json number,title,mergedAt,files,url`, then filtered by Code-tab UI/runtime/provider/test paths and capped to the newest 150 matching PRs. At refresh time the source pool had PRs through #286; the newest Code-tab PR is #286 (the merge of this stabilization effort itself) and the 150th matching PR is #91. Versus the prior refresh, exactly one PR entered the window (#286) and one left (#90); the path filter was verified to reproduce the prior documented window exactly before recomputing. The current 150 PRs are #286, #281, #279, #278, #277, #276, #275, #274, #273, #272, #271, #270, #269, #268, #267, #266, #264, #263, #261, #260, #259, #256, #255, #254, #253, #249, #248, #247, #246, #245, #244, #243, #242, #241, #240, #239, #237, #236, #235, #234, #233, #232, #231, #230, #228, #227, #226, #222, #221, #220, #219, #218, #217, #216, #215, #214, #213, #212, #211, #210, #209, #208, #207, #206, #204, #203, #202, #201, #200, #199, #197, #196, #195, #194, #190, #189, #188, #186, #185, #184, #183, #182, #181, #180, #178, #176, #174, #173, #172, #171, #170, #168, #167, #166, #165, #164, #161, #160, #159, #156, #155, #154, #153, #152, #150, #149, #148, #146, #145, #143, #142, #141, #139, #136, #135, #134, #133, #132, #131, #130, #129, #128, #120, #119, #118, #116, #115, #113, #112, #111, #110, #109, #108, #107, #106, #105, #104, #103, #102, #101, #100, #99, #98, #97, #96, #95, #94, #93, #92, #91.

PR #286 merged the previously-WIP Code-tab stabilization onto `main` at `d215a7c5`. Every "current work" / "current WIP" statement in the tables below now describes merged `main` behavior; the reproducible regression path `script/run_code_tab_unit_checks.sh` re-passed on clean `main` on 2026-06-10 (SessionLauncherModelTests 20/20, WorkspaceTabsTests 36/36, WorkbenchStateTests 20/20, ChatSendPerProviderTests 15/15, AgentControlServerChatRouteTests 35 executed / 4 expected live-provider skips / 0 failures) in `/tmp/cw-unit-checks-main-d215a7c5.log`.

Local PR evidence cache: `/tmp/continuum_merged_prs_300.json` was generated during this refresh and contains the PR numbers, titles, merged timestamps, URLs, and changed file lists used for the filter.

## Global Contracts

| Contract | Intent | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|
| Response latency | Every click, toggle, picker selection, shortcut, and input action gives visible feedback within 100 ms. Slow work continues asynchronously. | Several paths are already optimistic in source (`+` creates provisional row, model picker mutates local store, composer injects pending bubble). Current tests measure provisional provider/model toggles, terminal-tab pending feedback, draft tab open/select/close feedback, draft model-picker selection feedback, bulk archive feedback, loaded sidebar search/status/archive projection feedback, and compact tab-strip width feedback under 100 ms; rendered UI coverage also verifies unbounded tab-plus, unbounded `Cmd+T`, picker search/favorite/command-digit/effort flows, and terminal shortcut behavior. Coverage is not yet exhaustive for every element. | #281, #245, #242, #150, #149, #148, #145, #120, #118 | Extend the reproducible latency harness to every row in this spec, especially remaining menu/pane controls and provider send/stop paths. |
| Provider privacy and opt-in | Opening Code must not read other apps' data or trigger folder/keychain prompts unless the user opts in or performs a relevant action. Providers visible in Code are filtered by enablement, and logs must not expose credential-derived token lengths, prefixes, fingerprints, prompt text, provider response bodies, trailer text, relay response bodies, or shell stdout/stderr bodies. | `ProviderEnablement`, gated discovery, local loopback token usage, and enabled-provider model catalog filtering are present in source. Current focused redaction coverage removes and guards token length/fingerprint/prefix logging in Anthropic polling, Claude token auto-import, Watch token receive, live-activity APNS pusher, APNS gateway, and APNS device-token store paths. Additional public-diagnostics coverage redacts Anthropic/Antigravity/Codex provider body previews, Cursor trailer details, relay grant error bodies, SidecarAsk prompt text, and git/whois stderr logs. Current no-spend XCTest coverage also gates Claude token auto-import, provider usage pollers, provider runtime warmers, mobile-command audit replay, and session/workspace/repo-env stores away from real user data unless live-provider spend is explicitly opted in. Full startup side-effect coverage for production launch remains pending. | #278, #222, #221, #276 | UI and route tests for disabled provider hidden/blocked states, production no-startup-prompt side effects, and broader whole-app log-redaction coverage. |
| Conductor-like structure | Code tab is a workbench: repo/worktree navigation on the left, model/session plurality in tabs, chat/composer in center, optional review pane on right. | Current source implements two-level repo -> worktree rows, workspace tab strip, center thread, right review pane, terminal/document tabs. Sidebar worktree selection now uses one active workspace key across session, draft, terminal, and document tabs instead of raw `openSessionId`, so selecting an unsent draft no longer leaves selection ambiguous or highlights a stale worktree. The workspace tab source now keeps every same-worktree model session; focused coverage creates six provider sessions in one worktree and verifies the tab source does not cap at two. Rendered UI coverage now verifies all six same-worktree provider/model session tabs render and that switching crowded tabs updates the center header session/provider/model. The rendered tab strip now compacts chat-tab labels while sizing the scroll region to keep `+` adjacent when tabs fit and visible when overflowed tabs scroll. | #281, #279, #269, #245, #174, #173, #100, #99, #98 | Add broader UI tests for multiple visible worktrees and close/dedupe behavior on crowded tab rows. |
| Provider matrix | Every user-selectable Code provider/model path works end to end: Claude, Codex, Gemini/Antigravity, OpenCode/OpenRouter, Cursor, Grok, with enabled-provider filtering. | `AgentKind.allCases` and `SessionLauncherModel.selectableAgents` include Claude, Codex, Gemini, OpenCode, Cursor, Grok when enabled/ready. Current XCTest coverage derives every bundled Code model from the catalog and verifies provisional launch state plus final launch request provider/model/effort propagation. Shared-harness fake coverage verifies Codex, Gemini/Antigravity, Cursor, and Grok prompt delivery, selected model/effort start parameters, stream completion, and provider error projection. Daemon route coverage verifies `userComposerFirstTurn` first prompts with `asFollowUp == false` reach fake live harness bridges for those same providers instead of being queued or blocked, verifies scheduled user follow-ups reach those same live bridges while rendering the user row and streaming state, and verifies Stop calls the live harness driver cancel path while restoring local Send/Stop state. Non-harness fake coverage now verifies Claude PTY JSONL prompt/reply/usage/lifecycle projection and OpenCode serve SSE prompt/reply/error/lifecycle projection. Dynamic live-catalog replacement coverage verifies OpenRouter and Cursor live rows replace stale fallback picker models and clear unsupported stale effort. Draft first-send planner matrix coverage now verifies every bundled Code provider/model preserves the selected provider/model/worktree, clears unsupported effort, remains first-turn, and passes the provider-prompt guard. Bound-session first-turn coverage verifies a connecting empty provider transcript still shows Send/first-turn instead of Stop/Queue, while optimistic pending or existing transcript turns remain interruptible. Rendered no-spend composer matrix coverage now renders every bundled Code provider/model in idle Send and running Stop states and asserts the primary action stays icon-only. OpenCode now has a gated live-drive case for real `opencode serve` session creation, prompt POST, SSE reply projection, and turn completion; it compiled and skipped when live-spend gates were absent. Live provider execution remains pending. | #281, #279, #278, #247, #244, #219, #181, #165, #154, #135, #134, #131, #130, #129, #128, #96, #94, #80, #74, #73, #72 | Add remaining live send/stop smoke coverage. |
| No silent provider prompts | Background probes, stale persisted sends, and startup checks must not send synthetic prompts to providers. Explicit user prompts must pass. | `ProviderPromptGuard` and route tests exist. Draft first-send planner matrix coverage now verifies every bundled Code provider/model uses `userComposerFirstTurn`, does not set `sendAsFollowUp`, and is accepted by the prompt guard. Bound-session first-turn coverage now also verifies a connecting empty transcript is not classified as an active follow-up turn merely because the provider state says streaming. Scheduled follow-up route coverage now verifies `.scheduledUserFollowUp` prompts reach Codex, Gemini/Antigravity, Cursor, and Grok live harness bridges, legacy synthetic scheduled prompts are blocked before provider delivery, and stale managed sessions return `acp_session_not_live` instead of falling back to a legacy prompt path. Remaining gap is live provider first-send/recovery smoke coverage. | #277, #270, #222 | Live provider first-send recovery and no-spend startup side-effect coverage. |

## Primary Navigation And Titlebar

| Element | Designed behavior | Inputs and outputs | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|---|
| Top nav `Code` tab | Switch primary dashboard to Code workbench. Layout starts flush under the hidden native titlebar. | Click `Code` or View menu `Code`; output is Code tab active, Code titlebar actions visible. | `TahoeDashTab("Code")` and View menu post tab switch. UI tests currently open via `dash.tab.code`. | #42, #40, #39, #99, #98 | UI test click/menu/shortcut focus should land on Code without stale tab content. |
| Titlebar Code filters button | Focus Code sidebar search/filter area without opening Settings. | Click sliders button; posts `.focusSidebarSearch`. | Source posts search focus notification and exposes `code.titlebar.focus-filters`; focused UI coverage verifies typing after click lands in sidebar search. | #55, #120 | Broaden latency measurement to assert visible focus feedback within 100 ms. |
| Titlebar right-pane menu | Open any right pane tab or collapse pane. | Menu items Plan, Diff, Terminal, Sources, Artifacts, PR, Browser, Collapse. Output posts `.openCodeReviewPane` or `.toggleCodeReviewPane`. | Source menu rows now expose stable IDs, and focused UI coverage clicks every row through the rendered titlebar menu. The review pane exposes an accessibility-selected marker so the test verifies Plan, Diff, Terminal, Sources, Artifacts, PR, and Browser selection plus Collapse hiding the pane. | #47, #25, #210, #242 | Add latency assertion for menu open/selection feedback and broader pane-content assertions. |
| View menu `Search Code` / Cmd+K | Switch to Code and focus real sidebar search. | Cmd+K, output Code tab active and search field focused. | Implemented in app command; focused UI coverage verifies Cmd+K focuses Code sidebar search from a selected worktree. | #55, #42 | Add direct menu-row coverage and latency assertion. |
| View menu `New Workspace Chat Tab` / Cmd+T | Open draft chat tab in current workspace. | Cmd+T, output a draft tab with inherited defaults. | `WorkspaceTabSpawnObservers` and hidden shortcut both route to `openDraftWorkspaceTab`; command handling now appends from a selected session or a selected draft, so the path does not stop at one unsent draft. The root command registry now also contains `code.newChatTab`, so the command palette and shortcut override monitor cannot drop the action. Focused UI coverage verifies repeated `Cmd+T` appends while a draft is already selected. | #188, #174, #185 | Add direct menu-row coverage and defaults assertions around inherited active-session/draft configuration. |
| View menu `New Workspace Terminal` / Shift+Cmd+T | Open/select terminal tab for active worktree. | Shift+Cmd+T, output immediate pending terminal tab, then a promoted direct-shell terminal pane for eligible local worktree sessions; legacy tmux-backed records remain blocked. | `openOrCreateWorkspaceTerminalTab` now selects a pending tab before shell spawn, creates an explicit direct-shell pane asynchronously, and no longer depends on agent `supportsTerminal`. The root command registry contains `code.newTerminalTab`, and terminal source resolution works from a selected draft by picking an eligible same-workspace sibling session. Unit coverage proves pending feedback under 100 ms; UI coverage verifies the tab/surface reaches `Terminal connected`. | #188, #174, #239, #248 | Add menu-row coverage in addition to the verified shortcut path. |
| View menu `Toggle Review Pane` | Expand/collapse right review pane without losing chat scroll position. | Menu item, output pane visibility toggled; chat stays bottom-pinned if it was pinned. | Source posts notification and `ChatThreadScroll` observes pane visibility. | #210, #47 | UI test toggles while at bottom and asserts latest message remains visible. |

## Sidebar

| Element | Designed behavior | Inputs and outputs | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|---|
| Search input | Filter repos, worktrees, sessions, and recent JSONLs by case-insensitive text. Clear button resets. | Type query, clear button; output filtered sidebar. | Real `TextField` with focus notification, clear button, filtered projection. Unit latency coverage verifies loaded search/status/archive sidebar projections stay under 100 ms on a 500-session fixture. | #55, #106, #148, #213 | Add UI-level search/filter latency proof on the rendered sidebar. |
| Filter/group/sort menu | Change status filter, grouping, sorting, refresh repo list, reset filters. | Menu selections; output persisted sidebar projection changes. | Menu exists; added `code.sidebar.filter` ID in current work. | #106, #148, #213, #214 | UI test each menu path updates visible rows; refresh gives visible progress. |
| Status buckets | Quick-filter Active, Review, Done, Archive buckets. | Click bucket; output status filter toggled. | Source has bucket controls with stable IDs. Current work syncs the Archive bucket with the archived-session gate, verifies archived worktrees reappear under Archive after hover archive, and adds deterministic bucket-state coverage for Active, Review, Done, Archive, toggle-off-to-All, cross-bucket switching, and Archive-only archived visibility. The 500-session sidebar projection latency gate covers search/status/archive projection feedback under 100 ms. | #106, #161, #234 | Add direct UI click-state coverage if product requires visual selected-state assertions beyond the source-backed state machine and projection tests. |
| Add project menu | Three onboarding flows: Open project, Open GitHub project, Quick start. | Click folder-plus, choose row. Outputs folder picker, clone sheet, or quick-start sheet. | Source menu exists; added `code.sidebar.add-project` ID. | #199, #222, #221 | UI tests for menu rows and visible loading/error states. |
| Open project | Register a local repo without showing historical sessions under Managed. | Folder selection; output workspace row under Managed, clean slate. | Source calls `RepoOnboarding.openLocalFolder`; managed recents hidden by PR #214. | #199, #214, #213 | Route/unit tests plus UI fixture for new workspace row. |
| Open GitHub project | Clone via `gh`, with install/auth recovery buttons. | Spec input owner/repo/URL and destination; output clone progress, errors, registered repo. | `CloneRepoSheet` includes Choose, Clone, copy install/auth commands. | #199 | UI tests for sheet controls and disabled/loading states. |
| Quick start | Create folder, `git init`, register repo. | Name and parent folder; output new repo workspace. | `QuickStartRepoSheet` has name, parent, Choose, Create. | #199 | Unit/UI test for created repo and visible progress. |
| Repo disclosure | Expand/collapse a project. | Click chevron/header; output worktree rows shown/hidden. | Source `repoHeader` toggles `expandedRepoKeys`. Added `code.repo.toggle` ID. | #245, #269 | UI test toggle without losing selected session. |
| Repo plus | Instant new worktree/session in repo. Option-click opens full customization sheet. | Click `+`; output provisional worktree row within 100 ms, setup trail, then real session. Option-click output new-session sheet. | Source `quickSpawnInRepo` creates provisional session and reserved worktree slug; earlier bug reports showed stale branch opening, current code now extracts the daemon-free provisional row/selection step into `createQuickSpawnProvisionalSession` so it can be regression-tested without provisioning a real worktree. Direct `xcrun xctest` evidence proves the provisional row targets the reserved worktree, avoids the stale branch, leaves one active worktree selection, and stays under the 100 ms feedback budget. Added `code.repo.new-session` ID. | #281, #279, #245, #231, #227, #173 | Add rendered UI coverage for no stale branch / no double selection and keep investigating the local `xcodebuild test` orchestration hang. |
| Repo settings gear | Open repo actions: new session, archive all sessions, archive entire repo, repo settings link, remove repo. | Menu rows; outputs respective registry/workspace actions. | Menu rows expose stable IDs for new session, archive all, archive repo, settings, and remove. Managed-workspace lookup compares raw, symlink-resolved, and normalized repo keys so `Remove from list` does not disappear for canonicalized workspace sections. Focused UI coverage verifies row reachability, `New session here` opening the Code launcher, Archive All archiving sessions without removing the workspace record, Archive Entire Repo archiving sessions and removing the workspace record while keeping archived sessions recoverable, and Remove deleting only the workspace record without archiving/deleting session history. Bulk archive is coalesced and has a 250-session feedback benchmark under 100 ms plus replay coverage. Worktree reclaim requires explicit Continuum-owned provisioning metadata. Linked settings internals are not counted as Code-tab release evidence. 2026-06-10: the menu's `New session here` had silently degraded into an instant quick-spawn (the launcher behavior was only ever an accidental fallback that the key-canonicalization fix removed); the row is now explicitly wired to `prepareNewSession(in:)` so the gear menu opens the launcher while the adjacent `+` keeps instant quick-spawn. | #281, #245, #201 | Add any remaining confirmation/undo polish if product requires stronger destructive-action guardrails. |
| Worktree row | Leaf row for a branch/worktree; opens most recently active model session; model plurality appears in tabs, not sidebar children. | Click row; output center opens worktree, active session tab selected. | Source groups sessions by workspace path and opens newest active. Row highlight and archive-hover persistence are now driven by `activeWorkspaceKey`, covering normal sessions plus selected draft, terminal, and document tabs. The row also exposes `code.worktree.row.selected` and a selected accessibility value for UI regression coverage. | #269, #245, #174 | UI test with multiple models under one worktree shows one row and multiple tabs; add rendered multi-worktree selected-row count coverage. |
| Worktree hover archive | Reveal one archive button on hover, no row height jump; archive every session in that worktree and reclaim owned checkout to Trash. | Hover, click archive; output row archived/removed, undo toast, reclaim task. | Source has `SessionHoverActions`; current work added IDs and bulk archive path. Unit coverage verifies bulk archive replay, 250-session feedback under 100 ms, and reclaim eligibility requiring `ownsWorktree` plus matching ownership-marker metadata. UI coverage verifies hover/selection reveals the action, clicking persists `archivedAt`, removes the row from the default sidebar, and shows it again in the Archive bucket. | #281, #279, #233, #185 | Add undo-toast click coverage. |
| Session row context menu | Pin/unpin, unread, mute, snooze, color tag, pop out, compare, copy ID, reveal JSONL, open PR, rename, archive/unarchive, new sub-chat, end session. | Right-click row; output presentation or registry side effect. | Source menu exists on session rows. Worktree-first layout means this mostly applies to external/history rows or legacy grouping. | #160, #185, #109, #111 | UI/context menu tests for non-destructive rows; unit tests for presentation state. |
| Footer New session | Open full new-session sheet for arbitrary repo/customization. | Click footer or Cmd+N; output new session launcher. | Source `model.prepareNewSession(in:nil)`, UI test covers Cmd+N. | #199, #185, #42 | Verify no stale branch/session opens and fields reflect selected repo/defaults. |
| History/Discover parallel sessions | Keep external scans opt-in; show external active/history only after opt-in. | Click discover/history controls; output external JSONLs shown read-only. | Source supports discovery gating and external sections; exact current discover control needs UI pass. | #222, #273, #276 | Verify Code open causes no prompt; opt-in populates external rows. |

## Workspace Tabs And Center Header

| Element | Designed behavior | Inputs and outputs | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|---|
| Workspace session tabs | Show every model session on the selected worktree; clicking switches session without tearing down composer state incorrectly. | Click tab; output active session changes. | Source `WorkspaceTabStrip` dedupes session IDs, sorts by createdAt, and exposes selected session/provider/model state for the rendered tabs. `WorkspaceTabsTests/test_sameWorkspaceModelSessionTabsAreNotCappedAtTwo` now creates six same-worktree model sessions across Claude, Codex, Antigravity, Cursor, OpenCode, and Grok and verifies the tab source includes all six. `test_workspaceTabStripCompactsBeyondTwoTabsOnMinimumCenterWidth` verifies the rendered chat tab sizing is not visually capped at two on the minimum center width. `CodeTabHoverShortcutUITests/testWorkspaceSessionTabsRenderEverySameWorktreeModelSessionAndSwitchHeader` renders those six same-worktree provider/model tabs, then switches Cursor and Grok tabs and verifies the center header uses the selected session/provider/model. | #281, #279, #174 | Add close/dedupe/accessibility coverage around crowded horizontal tab rows. |
| Tab close button | End/close the selected session tab. | Click x; output `endSession`. | Source calls `model.endSession`. | #174, #109 | Verify confirmation/destructive behavior and cleanup. |
| Tab plus button | Create a Chat workspace tab beside existing tabs; secondary/context actions may expose Terminal where available. | Click plus; output appended draft chat tab. Context-click plus; output Chat and Terminal menu rows, and Terminal opens an immediate terminal workspace tab. | Focused UI coverage verifies repeated direct `+` clicks append draft tabs from the Code tab instead of replacing the existing draft, with the tab strip reporting the rendered session-plus-drafts count; the latest source change keeps `+` outside the scrollable tab items and sizes the scroll region so `+` stays adjacent when tabs fit, then remains hittable after overflow and can append a sixth draft. Focused source coverage verifies the tab strip compacts enough for four chat tabs plus `+` at 420 pt, then scrolls for larger counts instead of dropping tabs. Focused secondary-action UI coverage verifies the `+` context menu exposes Chat and Terminal rows with stable targets, and Terminal opens `code.workspace.tab.terminal`, renders `code.workspace.terminal.surface`, and shows connection feedback. | #279, #188, #174 | Add close/dedupe/accessibility coverage around crowded horizontal tab rows. |
| Draft tab | Fresh chat tab inherits current worktree context and model defaults until first send. Multiple unsent drafts may stay open in one workspace. | Cmd+T/tab plus; output an appended draft composer tab, not replacement of an existing draft. | Source uses `workspaceDraftTabs` plus `selectedWorkspaceDraftTabId`; unit coverage verifies three drafts append from a session, sibling drafts append from an already selected draft, selection survives, closing one draft leaves the rest, and per-draft composer text/model/effort state stays isolated across draft switches. Unit coverage also verifies every bundled provider/model selected on a draft sends into the same worktree as `userComposerFirstTurn`, not a follow-up, clears unsupported stale effort, and passes the provider-prompt guard. Unit latency coverage verifies opening 50 drafts, selecting drafts, closing 25 drafts, and applying 49 draft model-picker selections each stay under 100 ms worst-case. The draft composer no longer publishes model changes during SwiftUI view construction. UI coverage verifies repeated `+` clicks and repeated `Cmd+T` create additional draft tabs, rendered picker selection can switch a draft to Antigravity Gemini 3.5 Flash Thinking without opening a stale session, and picker search/favorite/shortcut/effort actions keep the active workspace on the draft. | #188, #174, #281 | Add full daemon first-send verification after switching models. |
| Terminal tab | Raw PTY terminal for the worktree. | Shift+Cmd+T or secondary tab action; output a fresh pending terminal tab immediately for each request, then direct-shell panes for eligible local worktree sessions. | Current work creates pending tabs before shell spawn, promotes them to explicit direct-shell panes, keeps legacy tmux sessions blocked, resolves a terminal source from the selected draft when needed, and no longer applies the config-swap throttle or seven-pane daemon ceiling to terminal tabs. Nil-pane terminal WebSocket subscriptions now create/reuse a direct shell pane before any Claude command PTY path, so the Code Terminal surface opens a worktree shell instead of attaching to the Claude agent transport. Unit/route coverage opens eight terminal tabs and eight direct panes without pending-tab reuse, verifies PTY input/output, verifies Claude Code terminal WS direct-shell routing, verifies reconnect/failed presentation state, and UI coverage verifies the workspace shortcut, tab-strip secondary action, and right-pane Terminal routes all reach `Terminal connected` instead of passing on a stuck starting state. Live socket-drop UI coverage forces already-rendered workspace and right-pane terminals from `Terminal connected` to `Terminal reconnecting` and back to `Terminal connected`. Terminal surfaces now expose contained accessibility state markers so reconnect regressions can be asserted against the specific pane. | #248, #239, #217, #216, #188 | Add close/dedupe coverage around crowded terminal rows. |
| Document tab | Generated Markdown artifact opens as read-only document tab. | Click generated artifact; output markdown document tab with Open/Reveal/Copy/Refresh actions. | `MarkdownDocumentTabView` supports Open in Editor, Reveal in Finder, Copy Path, and Refresh actions, while `WorkspaceTabStrip` exposes `code.workspace.tab.document`. Focused unit coverage opens a document from a worktree, normalizes relative/absolute paths, dedupes repeated opens to one tab, closes back to the origin chat, and verifies document paths are allowed only under the worktree or generated-doc roots. | #206, #215 | Add rendered UI coverage for toolbar actions and document-tab close in the tab strip. |
| Transcript density menu | Compact/Balanced/Detailed transcript density. | Click header density icon; output row density changes. | Header rows expose stable IDs and selected-density markers. Focused UI coverage verifies Balanced initial state, then Detailed and Compact selection feedback. 2026-06-10: a container-level `code.center.header` identifier (added for the header-switch test) had stomped every child header identifier, breaking density/More-actions/checkpoint AX addressability; fixed with `.accessibilityElement(children: .contain)` on the header container. | #242, #210 | Add persistence coverage across session/workspace reloads. |
| More actions menu | Open terminal, schedule follow-up, create/restore checkpoint, pop out, archive, end session. | Click ellipsis; output corresponding sheet/action/state. | Menu rows expose stable IDs for Terminal, Schedule Follow-up, Create Checkpoint, conditional Restore Latest Checkpoint, Pop Out, Archive, and End Session. Focused UI coverage verifies safe rows, schedule sheet open/dismiss, Terminal tab/surface routing, Archive hiding the active session while keeping it recoverable under Archive, End Session deleting a non-owned fixture session without removing the managed workspace record, checkpoint create/restore round-tripping an isolated git repo from the header menu, blocked dirty-tracked restore disabling Restore while Cancel preserves the worktree, and blocked untracked-overwrite restore disabling Restore while Cancel preserves the file. | #239, #188, #185, #109, #111 | Add broader destructive-action confirmation/undo polish if product requires it. |
| Rename shortcut | Shift+Cmd+R opens rename dialog for active session. | Shortcut; output shared rename alert. | Current UI test covers this path. The root command registry now also contains `session.rename` and routes it to the shared rename notification. | #185, #160 | Keep regression test. |
| Pop out | Open focused session in separate window and protect chat store from eviction. | Menu/context action; output separate window. | Source posts `.popOutSession`. | #111, #109 | UI test window appears and close does not kill main session. |

## Transcript, Queue, Checkpoints

| Element | Designed behavior | Inputs and outputs | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|---|
| Chat transcript | Render user/assistant/tool/diff/artifact turns, strip synthetic system XML, collapse tool runs, keep latest pinned on pane toggles. | Provider transcript events; output chat rows. | Source implements projection and strip logic; focused tests exist for some parsers. | #215, #212, #211, #210, #207, #200, #110 | End-to-end fixture per provider plus scroll/pane UI tests. |
| Find in transcript | Search within transcript and navigate previous/next. | TextField and arrow buttons/menu commands; output highlighted match and scroll. | Source controls exist. | #216, #42 | UI tests for find, next, previous with stable IDs. |
| Jump to latest | Re-pin scroll to latest turn. | Button/shortcut; output scroll bottom. | Source includes bottom anchor and jump logic. | #210, #215 | UI test after scrolling up. |
| Preview chip | Completed assistant turn with local URL opens full-workspace Browser preview. | Click Preview; output Browser fills center, sidebar remains visible, Back to Chat returns. | Current UI test covers Preview/Back/URL/status/restart. | #266, #206 | Extend to annotation/comment add flow. |
| Queued follow-ups panel | While a turn is running, queued prompts show editable rows with send-now/delete and clear. | Queue action, edit field, send icon, trash, clear. | Source panel exists; current first-message queue bug fixed in current work by first-turn/follow-up classifier. Draft first-send unit coverage verifies `sendAsFollowUp == false` and `userComposerFirstTurn` for every bundled provider/model after provider/model switches. Daemon route coverage now verifies Codex, Gemini/Antigravity, Cursor, and Grok first-turn sends POST through `/sessions/:id/send` with `asFollowUp == false`, reach the selected live harness, echo the user row, and enter streaming state rather than landing in queued follow-ups. Rendered no-spend UI coverage verifies a draft first prompt stays in the composer, enables the icon-only send target, and does not expose the queued-followups panel before send. | #277, #150, #182 | Add rendered coverage proving follow-up rows appear only during an active turn, plus post-send UI coverage for the first-turn daemon path. |
| Checkpoint strip | Shows latest user checkpoint with restore action after lifecycle checkpoints; safety checkpoints created during restore preview stay recoverable internally but do not replace the user-visible latest checkpoint. | Click Restore; output restore preview sheet, then restore. | Source has strip and restore sheet with stable sheet/status identifiers. Focused UI coverage creates a manual checkpoint in a temp git repo, verifies a checkpoint ref plus header feedback and strip render, commits a later file state, opens Restore latest from the header menu, sees the restore preview, clicks Restore to checkpoint, and verifies the tracked file returns to the checkpoint contents. Adjacent UI coverage verifies dirty tracked changes and untracked overwrite risk both surface blocking reasons and dirty-status cues, leave Restore disabled, and Cancel dismisses without changing file contents or dirty/untracked status. Unit coverage verifies `restore(plan:)` revalidates untracked overwrite risk after preview and `latestCheckpoint` ignores safety restore snapshots. | #281, #111 | Add broader lifecycle/checkpoint API coverage if remote clients need safety-checkpoint visibility separated from user checkpoints. |
| Plan halo | Inline pending plan with Refine and Approve controls. | Click Refine/Approve; output composer primed or approval sent after checkpoint. | `InlinePlanHalo` now exposes stable `code.plan-halo` targets plus tested action descriptors for Refine, Edit plan, and Approve & run. The approve descriptor follows read-only state, and the existing container approval path creates a checkpoint before `approvePlan`. | #161, #109, #104 | Add rendered UI click coverage for Refine priming and Approve checkpoint side effects, plus provider-mode coverage. |

## Composer

| Element | Designed behavior | Inputs and outputs | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|---|
| Prompt text field | Multi-line prompt entry; Enter behavior follows Code conventions; supports slash/skill commands and mentions. | Type text, slash, `@`; output draft text, mention/command palette. | Source `ComposerInputCore`, command and mention pickers exist. | #232, #236, #200, #115 | UI tests for text, slash palette, mention selection, no clipping. |
| Attachment button | Opens NSOpenPanel or handles paste/drop; stages files before send. | Click `+`/paperclip, paste/drop; output attachment chips and staged refs. | Source attach control exists with `code.composer.attach`. | #42, #67, #110, #111 | UI/unit tests for file staging, remove chip, first-send migration. |
| Prompt history | Opens saved prompts, lets user insert/copy/delete/save prompt. | Click history/up shortcut; output prompt popover/sheets. | `code.composer.history`, `code.composer.saved-prompts`, `code.prompt-history.search`, `code.prompt-history.done`, stable saved/history row IDs, and `code.prompt-history.empty` now identify the local prompt-history surface. Descriptor coverage verifies saved/history filtering, row IDs, empty state, and save-prompt enablement under 100 ms. `CodeTabHoverShortcutUITests/testPromptHistoryRenderedSheetSearchUseCopyAndDeleteRows` seeds the real `session-presentation.json` store and now passes rendered search, row use, copy, delete, and persistence in `/tmp/clawdmeter-ui-composer-sheets.log`. | #111 | Keep focused rendered coverage with future prompt-history changes. |
| Paste ANSI-stripping | Paste terminal text without ANSI noise. | Click paste action; output cleaned text. | `code.composer.paste-ansi` now targets the action. Composer paste delegates through `AnsiStrip.plain`, so CSI, OSC titles/hyperlinks, bracketed-paste markers, and controls are stripped before the text replaces an empty composer or appends to existing text. Unit coverage verifies the cleaned paste lands under 100 ms, and UI-test build coverage verifies the rendered control stays addressable. | #111 | Add unlocked rendered click smoke if product requires end-to-end clipboard tap coverage. |
| Expanded editor | Opens expanded composer editor. | Click expand; output larger editor, Done closes. | `code.composer.expand` opens a sheet with stable `code.composer.expanded-editor`, input, title, save-prompt, and Done targets. Save Prompt uses the same tested non-empty prompt predicate as the saved-prompt menu. `CodeTabHoverShortcutUITests/testExpandedComposerEditorSavesPromptAndSavedPromptMenuReusesIt` now passes rendered open/edit/title/save/Done and saved-prompt menu reuse against the real presentation store in `/tmp/clawdmeter-ui-composer-sheets.log`. | #111 | Keep focused rendered coverage with future expanded-editor changes. |
| Dictation | Toggle system dictation. | Click mic or Ctrl+M; output dictation start/stop or settings prompt. | Source `SpeechDictation` integration. | #42, #111 | UI test with mocked dictation/service. |
| Permission mode chip | Click quickly toggles Plan <-> Accept Edits; menu/Shift+Cmd+1-4 selects Ask, Accept edits, Plan, Bypass. | Click/menu/shortcut; output local draft mode update or bound-session `PermissionModeStore` update and daemon mode swap. | The chip now uses an explicit primary button for quick flip plus a native chevron menu for the full mode list; the prior `Menu(primaryAction:)` implementation rendered as a macOS popup and did not quick-flip under UI automation. Menu rows expose stable IDs and stable shortcut digits by mode, so Cursor hiding Plan no longer shifts Bypass from slot 4 to slot 3. Focused UI coverage verifies Ask -> Plan -> Accept edits quick flip, `Cmd+Shift+1/2` on a draft composer, every menu row on a draft, and Bypass on a bound session opening a trust sheet whose Cancel path preserves the prior mode. Unit coverage verifies provider capability gating and stable Ask/Accept/Plan/Bypass slots. | #210, #42, #109 | Add rendered confirm/respawn and provider-specific daemon mode-swap coverage. |
| Model/effort chip | Rich vendor-rail model picker. Selecting a model updates model, effort, and provider/agent. Picker filters by enabled providers, live catalogs, favorites, and effort support. | Click chip, search/select/star/cmd digit/cycle effort; output local chip update and launch/session config update. | Source picker exists with `code.composer.model-effort`; current work verifies every bundled catalog model propagates through provisional launch state and final launch requests. Dynamic catalog unit coverage verifies live OpenRouter and Cursor replacement rows displace stale fallback picker selections and clear unsupported effort. Picker accessibility IDs expose rail, search, clear, row, favorite, and effort targets; focused UI coverage verifies `⌘1` selects the first row without leaving Code, effort menu changes update the chip, `⌘⌥E` / `⌘⌥⇧E` cycle effort up/down with visible chip feedback, search finds Claude Haiku, favorites persist into the Favorites rail, unsupported-effort Haiku clears stale effort, and a draft can switch from Claude to Antigravity Gemini 3.5 Flash Thinking while remaining on the draft tab. Unit latency coverage verifies draft model-picker selection feedback stays under 100 ms. | #279, #278, #220, #219, #115, #108, #106 | Add remaining coverage for the full provider send/stop matrix. |
| Context usage chip | Shows context-window ring only; popover shows context, cost, 5h/session, weekly, Cursor quota where available. | Click chip; output popover. | `code.composer.context-usage` now derives its ring from a tested context-only descriptor, so high 5h/weekly percentages cannot inflate the chip. The popover renders from tested row descriptors with stable `code.context-usage.row.*` targets for context, cost, plan header, Cursor included/extra/total/auto/API rows, generic 5-hour rows, and weekly rows. Focused rendered UI coverage now clicks the Code composer chip and verifies the popover plus context/cost rows appear from the descriptor-backed targets. | #209, #110, #256, #255 | Add broader rendered coverage for Cursor quota and generic plan-row variants if product requires visual assertions beyond descriptor coverage. |
| Send button | Bottom-right icon-only send. On first real prompt, send as first turn; on existing transcript, send as follow-up. Must inject pending bubble immediately. | Click/Command-Return; output pending bubble, daemon `/send`, streaming/completion/error. | Current work makes send icon-only and routes empty-state/draft sends through `EmptyStateFirstSendPlan`, which production code uses for spawn args and send classification. Unit coverage verifies every bundled provider/model selected on a draft keeps the selected provider/model/worktree, clears unsupported stale effort, passes `ProviderPromptGuard`, and sends as `userComposerFirstTurn`. Route coverage verifies the daemon delivers `userComposerFirstTurn` first prompts with `asFollowUp == false` to Codex, Gemini/Antigravity, Cursor, and Grok fake live harnesses and enters streaming state. Unit latency gates verify empty-state first-send `isSending` feedback and bound composer pending-bubble injection both stay under 100 ms. Rendered no-spend UI coverage verifies a typed draft first prompt enables the bottom-right icon-only send target without rendering queued follow-ups before send; the shared composer descriptor/render test verifies idle state resolves to icon-only `code.composer.send` and does not include legacy `$0.000`, `live`, or `tap to stop` text. The rendered matrix test now repeats that icon-only Send assertion and nonblank render for every bundled Code provider/model. | #281, #279, #277, #150, #67 | Add rendered post-send latency and live provider send coverage across provider matrix. |
| Stop button | While a turn streams, send button becomes bottom-right icon-only stop. | Click/Command-.; output interrupt route and visible stopped/error state. | Current work removes cost/live text and uses streaming state. Harness sessions cancel through the live bridge, Claude PTY writes ESC, stale managed sessions return `acp_session_not_live`, and OpenCode returns explicit `opencode_interrupt_not_supported` while locally restoring the Send/Stop turn state instead of masquerading as a retired legacy session. Route coverage now proves Codex, Gemini/Antigravity, Cursor, and Grok live-harness Stop calls the driver cancel path and restores local Send/Stop state. Rendered no-spend composer coverage verifies running state swaps Send for icon-only `code.composer.stop`, keeps Queue as a separate affordance, and renders a distinct nonblank running composer snapshot without legacy cost/live text. The rendered matrix test now repeats that icon-only Stop assertion and nonblank render for every bundled Code provider/model. | #279, #109, #67 | Add live-provider stop smoke coverage and UI coverage for visible stopped/error state after interrupt. |
| Queue follow-up button | While streaming, separate queue button queues draft without replacing stop. | Click/Option-Return; output queued row. | Current work separates from stop/send. | #277, #150 | UI test queue row appears and can dispatch/delete. |
| Pending message retry/dismiss | Failed or offline sends render retry/dismiss controls. | Click Retry/Dismiss; output retry send or clear pending. | Pending actions now use a tested descriptor list: sending shows no retry/dismiss, failed shows `Retry` plus dismiss, and queued-offline shows `Retry now` plus dismiss with the same stable accessibility targets. Store-level coverage verifies failed pendings stay visible until user action, retry preserves the same pending slot, clear drops the slot, offline queue captures/replays, retry re-hitting offline dedupes, queue cap fails loudly, and pending-bubble injection stays under 100 ms. | #150, #182 | Add UI click coverage for the visible retry/dismiss controls if product requires end-to-end button tap assertions beyond descriptor/store tests. |

## Right Review Pane

| Element | Designed behavior | Inputs and outputs | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|---|
| Gutter tabs | Collapsed rail opens Plan, Diff, Sources, Artifacts, Browser, PR, Terminal panes. | Click icon; output right pane expanded to selected tab. | Source exposes `code.review.gutter.*`; focused UI coverage verifies every gutter target when compact layout is active. | #47, #25, #242 | Add width/state assertions beyond target presence. |
| Review tab bar | Switches expanded pane tabs. | Click tab; output content switches; Diff widens pane. | Source exposes `code.review.tab.*` and `code.review.pane`; focused UI coverage clicks every tab and reaches the Terminal target. | #47, #25, #145 | Add content-specific assertions and Diff width behavior. |
| Plan pane | Shows pending/approved plan and approval state. | View/approve; output plan approval route. | `TahoeReviewPlanPane` now has a tested presentation model for pending, approved, todos fallback, and empty states. Pending plans show `code.plan-pane.approve`, wired through `WorkspaceReviewPane.onApprove`, so right-pane approval uses the same checkpoint-then-approve route as the transcript/composer path. Route coverage now posts `/sessions/:id/approve-plan` for a planning Claude Code session, verifies HTTP 200, clears pending plan text, persists the approved plan, seeds plan progress, moves the session to running, and respawns the Claude PTY using a fake binary. A rendered no-spend regression, `CodeTabHoverShortcutUITests/testPlanPaneApproveButtonApprovesRenderedPendingPlanAndCreatesCheckpoint`, now seeds a planning session plus fake Claude binary and compiles with the UI-test target. | #161, #109 | Run the rendered Plan approve click test on an unlocked desktop and record the pending -> approved visual transition plus checkpoint assertion. |
| Diff pane | Shows changed files, layout picker, next unviewed, mark all viewed, file/hunk actions, staged/unstaged controls. | Click controls; output presentation state/git actions/prompts. | `TahoeDiffPreviewPane` now exposes stable Code-tab targets for the toolbar, file/unviewed counts, layout picker, next unviewed, mark all viewed, file row actions, and hunk row actions. Focused descriptor coverage verifies enabled/disabled state for empty/populated toolbars, already-viewed files, and collapsed/expanded hunks without invoking git or destructive actions. Rendered UI coverage now opens a seeded dirty worktree Diff pane, clicks layout, next unviewed, mark all viewed, hunk collapse/expand, and Explain, verifies the disposition controls render enabled, and verifies Explain inserts a composer prompt. `GitDiffPane` now exposes stable `code.diff.git.*` targets for refresh, file/hunk stage/revert/unstage/trash actions, commit sheet controls, and error/loading states; temp-repo coverage verifies hunk stage/unstage use the correct index/worktree domains, and rendered UI coverage verifies file-level Stage and Unstage buttons mutate a seeded repo both ways. | #145, #216, #111 | Add destructive confirmation surfaces and live git outcome/error coverage. |
| Sources pane | Opens source entries from transcript/context. | Click source row; output file/browser open. | `TahoeSourcesPreviewPane` now exposes stable `code.sources.*` targets and descriptor coverage for the 14-row cap, file/URL subtitles, counters, and payload-backed accessibility values. Rendered UI coverage seeds `Read` and `WebFetch` transcript entries, verifies `notes.md` and `https://example.com/continuum-docs` rows, and clicks a source row through the open handler while suppressing external Finder/browser launches under UI tests. | #215 | Add live external-open smoke coverage if product requires verification beyond the in-app open handler. |
| Artifacts pane | Lists output artifacts, QuickLook/open actions. | Click artifact/open/close preview; output preview or external open. | `ArtifactsPane` now exposes stable `code.artifacts.*` targets for empty, grid, card, thumbnail, preview, and preview close states. Descriptor coverage verifies fixture artifact filename/path values, and rendered UI coverage seeds a `Write` artifact, verifies the `report.md` card, opens preview state, and closes it with Escape without invoking external apps. | #215, #206, #217 | Add visual QuickLook snapshot/open-reveal coverage if product requires more than preview state verification. |
| Browser pane | Embedded preview browser with Back/Forward/Reload/URL/Run/Stop/Restart/output/comment annotation. | Buttons/text fields; output web navigation/run profile/comment chip. | Current UI test covers full-workspace Browser basics; annotation not yet covered. | #266, #201, #222 | UI tests for all toolbar controls and annotation add/cancel. |
| PR pane | Manual URL/load/create/draft PR, open GitHub/checks/deployments, copy, rerun, ask agent, approve/merge. | Buttons/context menus; output PRCoordinator actions or prompts. | `TahoePRCompactPane` now exposes stable `code.pr.*` targets for empty-state manual URL/load/create/draft, title/subtitle, status/check rows, PR action menu rows, approve/request-changes/merge, progress, and error. Focused descriptor coverage verifies rerun-failed-check enablement from failed GitHub Actions run IDs, daemon-gated empty actions, merge enablement, TODO-blocked merge state, status/check row IDs, and check context actions without spawning `gh`. Rendered UI coverage now opens a loaded PR fixture, verifies title/subtitle/status/check/action targets, clicks Request changes to insert a composer prompt, and clicks Copy URL, Copy Number, and Ask agent to fix checks through the action menu. Deterministic coordinator coverage now verifies approve shell success/failure/missing-gh/parse-error paths, create failure error persistence, merge success snapshot updates, and merge failure error persistence without an immediate refresh clearing the action outcome. | #111, #217 | Add live GitHub/rerun smoke coverage for real `gh` outcomes. |
| Terminal pane | Inline terminal side pane reusing PTY-backed TerminalTabContainer. | Select Terminal tab; output live terminal or unavailable/offline placeholder. | Terminal availability now follows the fixed worktree-shell route rather than agent `supportsTerminal`; focused UI coverage clicks/selects the right-pane Terminal target and, for eligible local worktrees, requires the live terminal to reach `Terminal connected`. Shared terminal connection-state presentation now exposes reconnecting and failed socket states, and rendered right-pane terminal socket-drop coverage verifies connected -> reconnecting -> connected recovery. | #248, #239, #217 | Add close/dedupe polish if crowded terminal pane rows become hard to scan. |

## Provider And Model Matrix

| Provider | Code-tab intent | Current observed behavior | Source PRs | Verification required |
|---|---|---|---|---|
| Claude | Subscription-native direct PTY, first prompt survives startup MCP modal, plan/permission modes work, transcript JSONL mirrors. | Source has direct PTY, modal dismissal, strict MCP handling, prompt guard. Current route coverage verifies Claude Code plan approval restarts the PTY in run mode after persisting approved plan state and seeding plan progress. | #281, #279, #275, #248, #152, #129 | Fake PTY and live/smoke tests for first send, streaming, stop, rendered plan approve click, error. |
| Codex | ACP/app-server or CLI path, model picker includes OpenAI/ChatGPT defaults, JSONL/session resolver avoids stale rollout tailing. | Source includes Codex adapter and app-server unification. Current all-bundled launch test verifies Codex catalog rows route to the selected provisional session and final launch request. Shared-harness fake coverage verifies selected model/effort start parameters, prompt delivery, stream completion, and error projection. Daemon route coverage verifies a first-turn `userComposerFirstTurn` Codex prompt with `asFollowUp == false` reaches the fake live app-server bridge and streams locally. | #247, #245, #165, #130, #89 | UI new-session/toggle/send/stop test and live smoke coverage. |
| Gemini/Antigravity | ACP/headless harness and agy/Antigravity usage/model paths; selectable when enabled. | Source supports `.gemini`, Antigravity plan pane, agy ingestion. Current all-bundled launch test covers `gemini-3.5-flash-thinking` and the other bundled Gemini rows without reopening the stale selected session at the request boundary. Shared-harness fake coverage verifies selected model/effort start parameters, prompt delivery, stream completion, and error projection. Focused UI coverage verifies a draft can switch to Antigravity Gemini 3.5 Flash Thinking through the rendered picker and stay on the draft. Daemon route coverage verifies a first-turn `userComposerFirstTurn` Gemini/Antigravity prompt with `asFollowUp == false` reaches the fake live harness and streams locally. | #244, #154, #135, #94, #86, #81, #70 | UI send/stop coverage, live smoke coverage, and unavailable/CTA coverage. |
| OpenCode/OpenRouter | Uses `opencode serve` and CLI auth as source of truth; OpenRouter live model list appears; no redundant API-key prompt. | Source checks binary/auth readiness and live OpenRouter models. Dynamic catalog coverage verifies live OpenRouter rows replace fallback picker rows and clear unsupported stale effort. Fake SSE coverage verifies prompt/reply/error lifecycle projection. Send-body coverage verifies picked OpenRouter models are passed to OpenCode as `{providerID:"openrouter",modelID:<slug>}` while `opencode-default` omits the override. Stop/interrupt coverage verifies OpenCode returns explicit `opencode_interrupt_not_supported` and locally restores Send/Stop state. **2026-06-10: the first authorized live-spend run exposed that the live SSE reply path had never worked — four stacked transport/scoping/vocabulary/parse bugs meant a real `opencode serve` 1.16.2 reply never rendered in the Code tab. All four are fixed (1.16 event projection, per-directory `/event` streams, identity encoding, complete-JSON dispatch) and `testOpenCodeLiveDrive` now passes live in 5.96s with the reply token round-tripped; defect chain documented in `docs/CODE_TAB_PROVIDER_MATRIX.md`. Verbatim live-capture replay + deltas-only + idle-vs-interrupted regressions added to `ChatSendPerProviderTests`, and scoped-stream/LRU coverage to `OpencodeSSEAdapterTests`.** | #219, #113, #80, #74, #73, #72, #65, #131 | Keep the live-capture replay fixture in sync with future opencode upgrades. |
| Cursor | Selectable in Chat and Code by binary presence; live model probe populates picker; auth errors surface at spawn-time. | Source gates on binary presence and live catalog. Dynamic catalog coverage verifies live Cursor rows replace fallback picker rows and clear unsupported stale effort. Shared-harness fake coverage verifies selected model/effort start parameters, prompt delivery, stream completion, and error projection. Daemon route coverage verifies a first-turn `userComposerFirstTurn` Cursor prompt with `asFollowUp == false` reaches the fake live harness and streams locally. | #279, #219, #134, #96, #110 | UI selection/send/error test and live smoke coverage. |
| Grok | First-class ACP provider with usage analytics and context limits; selectable when enabled and `grok` binary exists. | Source includes `.grok`, `GrokHeadlessDriver`, usage/context rows. Shared-harness fake coverage verifies selected model/effort start parameters, prompt delivery, stream completion, and error projection. Daemon route coverage verifies a first-turn `userComposerFirstTurn` Grok prompt with `asFollowUp == false` reaches the fake live harness and streams locally. | #281, #259, #256, #254, #244 | UI selection/send/error test and live smoke coverage. |

## 2026-06-10 Status Update

The gaps below were written entering phase 2 and most are now closed:

- Live provider execution: DONE for Codex, Gemini/Antigravity, Grok, and OpenCode (after the OpenCode live-path defect chain was fixed — see the provider matrix). Cursor live execution is blocked by the account's real quota state ("Upgrade your plan to continue"), which the product surfaces as designed; rerun `LiveDriveTests/testCursorLiveDrive` after a plan upgrade.
- The rendered Plan-pane approve click (gap: "unlocked desktop") passed on 2026-06-10.
- The full `CodeTabHoverShortcutUITests` class now runs end-to-end as one pass; the first full-class run exposed and led to fixes for the center-header AX identifier stomp, the repo-settings launcher wiring, and cross-run composer-draft leakage (details in `docs/CODE_TAB_AUDIT.md`).
- ClawdmeterShared full suite: first recorded full pass (1423 tests, 0 failures).

## Known Current Gaps Entering Phase 2

1. Latency measurement now covers provisional provider/model toggles, terminal-tab pending feedback, bulk archive, draft tab open/select/close, draft model-picker selection, ANSI paste cleanup, loaded sidebar search/status/archive projection feedback, compact tab-strip sizing, and Workbench local controls including right-pane tab selection, review-pane visibility, transcript density, preview-chip routing, and queued follow-up insert/edit/remove. `WorkbenchStateTests` now includes a reusable `assertCodeTabFeedbackLatency` helper for local visible-state interactions, but no single reusable measurement yet proves the 100 ms feedback bound for every element.
2. Some destructive confirmation and live-outcome surfaces still need stable rendered coverage.
3. Full model matrix coverage is not complete. Current tests cover every bundled provider/model at the provisional launch request boundary, draft first-send planner boundary, and shared rendered composer Send/Stop boundary; they also cover dynamic live-catalog replacement for OpenRouter/Cursor picker defaults, rendered draft picker switching/search/favorite/command-digit/effort/effort-cycle paths, rendered draft first-prompt queue absence before send, shared-harness stream completion/error for Codex, Gemini/Antigravity, Cursor, and Grok, daemon first-turn `/send`, scheduled-follow-up, stale-managed-session, and Stop routing for those shared-harness providers, Claude PTY JSONL fake stream projection, OpenCode serve SSE fake stream/error projection, OpenCode selected-model send-body mapping, OpenCode unsupported interrupt/local Send-Stop restore, and a gated OpenCode live-drive case, but not the full per-provider live UI send/stop matrix.
4. Archive-all speed, reclaim eligibility, hover archive, repo-settings menu rows, repo-settings destructive behavior, center-header archive/end behavior, center-header checkpoint create/restore behavior, dirty-tracked blocked restore cancel behavior, untracked-overwrite blocked restore cancel behavior, and deterministic PR approve/create/merge outcomes have focused registry/UI/coordinator coverage.
5. The "damaged UI test runner" Gatekeeper/codesign issue can block XCUITest execution unless the runner is rebuilt/ad-hoc re-signed in the verification path.
6. Terminal reconnect behavior now has unit and rendered socket-drop UI coverage for both workspace and right-pane terminal surfaces; remaining terminal gaps are crowded-row close/dedupe polish.
