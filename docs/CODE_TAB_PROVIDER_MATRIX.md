# Code Tab Provider And Model Matrix

Date: 2026-06-09 05:20 ICT

Latest evidence update: 2026-06-10 (post-merge re-baseline on `main` at `d215a7c5` + first authorized live-spend smoke pass).

## 2026-06-10 Live Smoke Results (first authorized live-spend run)

Live spend was explicitly authorized by the user on 2026-06-10. Gates set: `CLAWDMETER_LIVE_VERIFY=1`, `CLAWDMETER_ALLOW_PROVIDER_SPEND=1`, `~/.continuum-live-verify`. Runner: direct `xcrun xctest` on the app-hosted `ClawdmeterMacTests` bundle after `build-for-testing`.

| Provider | Live result | Evidence |
| --- | --- | --- |
| Codex | PASS (20.7s) — real prompt, streamed reply | `/tmp/cw-live-drive-results.log` |
| Gemini/Antigravity | PASS (17.6s) — real prompt via `agy`, streamed reply | `/tmp/cw-live-drive-results.log` |
| Grok | PASS (23.7s) — real prompt via headless driver, reply | `/tmp/cw-live-drive-results.log` |
| Cursor | SKIP on first run (account quota: "Upgrade your plan to continue" — correct spawn-time surfacing) → **PASS (43.7s)** after aligning the CLI account. Root cause of the confusion: `cursor-agent` keeps its own login (was darshan@axtior.com, over limit) separate from the Cursor IDE session (harshit@axtior.com, Ultra) — and Continuum's Cursor usage/quota chip reads the **IDE** token while Code-tab spawns bill the **CLI** account, so the chip showed available limits while spawns were quota-blocked. Fixed operationally via `cursor-agent login` as the Ultra account; the identity split is a documented hazard (quota display and spawn billing can disagree whenever the two logins diverge). | first skip `/tmp/cw-live-drive-results.log`; pass `/tmp/cw-live-cursor-ultra.log` (`LIVE Cursor replied: CONTINUUM_LIVE_OK_…`) |
| OpenCode/OpenRouter | FAIL on first run → PASS (5.96s) after fixing four stacked live-path bugs (below). Reply token round-tripped through real `opencode serve` 1.16.2 SSE: `LIVE OpenCode replied: CONTINUUM_OPENCODE_LIVE_OK_…` | first fail `/tmp/cw-live-drive-results.log`; pass `/tmp/cw-live-oc-jsondispatch.log` |

Route-level live smokes through the real daemon also passed (gate note: the daemon-side spend check needs `CLAWDMETER_LIVE_PROVIDER_TESTS=1` in addition to the LiveDrive env, and `CLAWDMETER_PROVIDER_<ID>_ENABLED=1` overrides because providers default to opt-in-off under a bare test host): `test_liveCodeProviderSmoke_createsOwnedWorktreesWithoutPermissionPrompts` (Claude+Codex+Cursor+OpenCode+Gemini real worktree spawns, 73.5s — the live Claude coverage), `test_cursorRoute_createsSessionWhenProbePasses`, `test_openRouterRoute_createsOpenCodeSessionAndLiveSendWhenAvailable` (real OpenRouter live send, 3.6s), and `testFrontierGeminiBroadcastDrivesViaAgy` (real agy broadcast reply, 9.2s).

### OpenCode live-path defect chain (found + fixed 2026-06-10)

The first-ever live execution of the OpenCode path exposed that the SSE consume loop had **never** delivered a live event in any prior version — its 29 fake-event tests call the dispatch layer directly and so never exercised the transport/parse layers. Four independent, individually-fatal bugs, all fixed in `OpencodeSSEAdapter.swift`:

1. **Retired event vocabulary** — the adapter handled only `message.added` (opencode ≤1.15); live 1.16.2 streams `message.updated` + `message.part.updated` + `message.part.delta` + `session.status`/`session.idle`. Fixed with full 1.16 projection: per-message role tracking, per-part cumulative text buffers, completion-time token/model deltas, idle terminal safety net (which never overrides an interrupted/error state). Body projects once at completion because the chat store's staging pipeline is first-wins by message id.
2. **Directory-scoped `/event`** — opencode ≥1.16 scopes the event stream to one project directory per connection; the adapter's single unscoped subscription only ever sees the serve process's own cwd project. Fixed with one scoped stream per registered session directory (LRU-capped at 32 concurrent streams, per-directory reconnect/backoff, envelope-id dedupe so overlapping streams cannot double-count token deltas).
3. **Compressed SSE buffering** — URLSession's default `Accept-Encoding` (gzip/br) made opencode compress the stream, and the streaming decompressor buffered events indefinitely: HTTP 200, then zero bytes delivered ("connected but silent"). Fixed by requesting `Accept-Encoding: identity` on the event stream.
4. **Swallowed frame terminators** — `URLSession.AsyncBytes.lines` never yields blank lines, so the blank-line-terminated SSE frame parser dispatched nothing (observed live: 129 data lines consumed, zero events dispatched). Fixed by dispatching as soon as the accumulated `data:` payload parses as one complete JSON object.

Regression coverage added: `ChatSendPerProviderTests/test_opencodeServeSSE_v116_dispatchesLiveCaptureSequenceToReplyAndCompletion` (verbatim live-captured event sequence through `dispatchEvent`), `test_opencodeServeSSE_v116_deltasOnlyReplyProjectsAndCompletes`, `test_opencodeServeSSE_v116_sessionIdleDoesNotOverrideInterruptedTurn`, and `OpencodeSSEAdapterTests/test_register_startsScopedEventStreamPerDirectoryWithLRUCap`. A live-verify-gated stderr debug trace (stream lifecycle + event types only — never prompt/response bodies) stays in the adapter for future live debugging.

Scope: provider/model paths selectable from the current Code-tab source catalog and fixtures. This file records source-level availability and test evidence. It does not claim live provider success unless a live smoke test was actually run.

## Credential And Smoke-Test Policy

- Live smoke tests are intentionally gated because they use real provider CLIs and quota.
- Current live smoke command surface: `CLAWDMETER_LIVE_VERIFY=1 CLAWDMETER_ALLOW_PROVIDER_SPEND=1 xcodebuild test -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac)" -destination 'platform=macOS' -only-testing:ClawdmeterMacTests/LiveDriveTests`
- Current live smoke tests cover Codex, Antigravity/Gemini, Cursor, Grok, and OpenCode/OpenRouter when binaries/credentials exist. Claude is not in `LiveDriveTests` because it follows the subscription-native direct PTY path.
- This baseline did not run live spend tests. Credential status is therefore still `Not inspected` for provider-account validity unless noted otherwise, but local binary readiness was inspected without sending prompts or exposing tokens.
- Provider and relay logs must not emit credential-derived token lengths, prefixes, or fingerprints. Current focused guard: `CredentialLoggingRedactionTests/testProviderAndRelaySourcesDoNotLogTokenDerivedMaterial` in `/tmp/clawdmeter-credential-log-redaction.log`.
- Public diagnostics must not emit prompt text, provider response body previews, Cursor trailer text, relay grant response bodies, or shell stdout/stderr bodies. Current focused guard: `CredentialLoggingRedactionTests/testProviderAndCodeSourcesDoNotExposePromptOrResponseBodiesInPublicDiagnostics` in `/tmp/clawdmeter-public-diagnostics-redaction.log`; shared provider parser/source coverage passed in `/tmp/clawdmeter-shared-provider-redaction.log`.
- No-spend XCTest provider side effects are now guarded by `CredentialLoggingRedactionTests/testAppRuntimeDefersProviderSideEffectsUnderNoSpendXCTest` in the latest focused run. The gated live-drive compile/skip run in `/tmp/clawdmeter-livedrive-gated-after-mobile-outbox.log` shows Claude auto-import, provider pollers, provider runtime warmers, and mobile-command audit replay deferred before `LiveDriveTests` skip; it also uses isolated temp stores (`No sessions.json`) instead of the user's real Application Support session/workspace data.

## Local Non-Spend Readiness Probe

Probe command shape: resolve provider binaries on `~/.local/bin`, `~/.hermes/node/bin`, Homebrew, and system paths, then run only `--version`. This does not send prompts or validate paid provider account state.

| Provider | Binary probed | Local result | Credential/live status |
| --- | --- | --- | --- |
| Claude | `/Users/darshanbathija_1/.local/bin/claude` | Present, `2.1.169 (Claude Code)` | Account validity not inspected; live prompt not run. |
| Codex | `/Users/darshanbathija_1/.hermes/node/bin/codex` | Present, `codex-cli 0.137.0` | Account validity not inspected; live prompt not run. `/opt/homebrew/bin/codex` exists but fails under this shell with `env: node: No such file or directory`, so the Hermes runtime is the current usable probe path. |
| Gemini/Antigravity | `/Users/darshanbathija_1/.local/bin/agy` | Present, `1.0.6` | Account validity not inspected; live prompt not run. |
| OpenCode/OpenRouter | `/Users/darshanbathija_1/.opencode/bin/opencode` | Present, `1.16.2` | Auth providers not inspected; live prompt not run. |
| Cursor | `/Users/darshanbathija_1/.local/bin/cursor-agent` and `/Users/darshanbathija_1/.local/bin/agent` | Present, both `2026.06.04-5fd875e` | Account validity not inspected; live prompt not run. |
| Grok | `/Users/darshanbathija_1/.local/bin/grok` | Present, `grok 0.2.38 (7581931067b)` | Account validity not inspected; live prompt not run. |

## Provider Capability Summary

| Provider | Code-tab id | Runtime readiness gate | Supported Code capabilities | Unsupported-feature behavior | Mock/source coverage | Live smoke status |
| --- | --- | --- | --- | --- | --- | --- |
| Claude | `claude` | Provider enabled; direct Claude PTY/session path. Local binary present. | Chat, Code, live usage, historical usage, menu bar, mobile mirror, widget; Code Terminal opens direct worktree-shell panes instead of attaching to the Claude command PTY. | Models without effort support must clear effort. Legacy/tmux path remains Claude-specific. | Provider registry tests, catalog consistency tests, Claude adapter tests, terminal route coverage for PTY/direct shell surfaces, Claude Code terminal WS direct-shell regression, Claude Code approve-plan route restart/progress regression, all-bundled launch request matrix test, draft first-send planner matrix, rendered no-spend composer Send/Stop matrix, Claude PTY JSONL fake prompt/reply/usage/lifecycle projection test. | Not run; account validity not inspected. |
| Codex | `codex` | Provider enabled; `codex app-server`/harness path for live drive. Local Hermes binary present. | Chat, Code, live usage, historical usage, menu bar, mobile mirror, widget; effort supported on bundled Codex models. | Stale JSONL/session tails must not hijack new sessions; prompt origin guard must block synthetic prompts. | Provider registry tests, Codex adapter/source tests, all-bundled launch request matrix test, draft first-send planner matrix, rendered no-spend composer Send/Stop matrix, shared-harness fake prompt/stream/completion/error matrix, daemon first-turn route test, daemon scheduled-follow-up/stale-managed-session route tests, daemon live-harness interrupt/cancel route test, first-send/follow-up tests, `LiveDriveTests/testCodexLiveDrive` gated. | Not run; account validity not inspected. |
| Gemini/Antigravity | `gemini` | Provider enabled; `agy` binary/runtime for live drive. Local binary present. | Chat, Code, live usage, historical usage, menu bar, mobile mirror, widget; thinking variants supported; effort unsupported. | Effort must be nil; Antigravity unavailable should surface CTA, not stale session fallback. | Provider registry tests, Antigravity state/conversation/parser tests, all-bundled launch request matrix test, draft first-send planner matrix, rendered no-spend composer Send/Stop matrix, rendered draft picker switch test, shared-harness fake prompt/stream/completion/error matrix, daemon first-turn route test, daemon scheduled-follow-up/stale-managed-session route tests, daemon live-harness interrupt/cancel route test, `LiveDriveTests/testAntigravityLiveDrive` gated. | Not run; account validity not inspected. |
| OpenCode/OpenRouter | `opencode` | Provider enabled, `opencode` binary present, auth status non-empty; live OpenRouter model probe can replace fallback catalog. Local binary present; auth providers not inspected in this pass. | Chat, Code, historical usage, mobile mirror; OpenRouter fallback models include mixed effort support. | Not surfaced for widget/menu-bar/live-usage capability; models without effort support must clear effort; SSE send/error lifecycle must restore Stop/Send state rather than leaving the composer running; per-turn upstream interrupt is not exposed by OpenCode, so Stop returns `opencode_interrupt_not_supported` while locally restoring the Send/Stop state. | Provider registry tests, OpenCode adapter tests, OpenRouter model probe tests/source, provider-default tests, all-bundled launch request matrix test, draft first-send planner matrix, rendered no-spend composer Send/Stop matrix, live-catalog replacement normalization test, OpenCode serve SSE fake prompt/reply/error/lifecycle projection test, OpenCode SSE adapter regression tests, OpenCode interrupt unsupported/local-restore route test, `LiveDriveTests/testOpenCodeLiveDrive` gated. | Not run; auth providers not inspected. |
| Cursor | `cursor` | Provider enabled and `cursor-agent`/`agent` binary present; auth errors deferred to spawn-time. Local binaries present. | Chat, Code, live usage, historical usage, menu bar, mobile mirror, widget; live catalog can replace fallback Auto model. | Effort unsupported for fallback Auto; logged-out CLI should surface auth error at spawn-time. | Provider registry tests, Cursor catalog/parser/adapter tests, ACP support tests, all-bundled launch request matrix test, draft first-send planner matrix, rendered no-spend composer Send/Stop matrix, live-catalog replacement normalization test, shared-harness fake prompt/stream/completion/error matrix, daemon first-turn route test, daemon scheduled-follow-up/stale-managed-session route tests, daemon live-harness interrupt/cancel route test, `LiveDriveTests/testCursorLiveDrive` gated. | Not run; account validity not inspected. |
| Grok | `grok` | Provider enabled and `grok` binary present. Local binary present. | Chat, Code, live usage, historical usage, menu bar, mobile mirror, widget; bundled models support effort. | Missing binary should hide/disable launch path; quota/plan errors should skip live tests or surface as provider errors. | Provider registry tests, Grok analytics/headless driver tests, ACP support tests, all-bundled launch request matrix test, draft first-send planner matrix, rendered no-spend composer Send/Stop matrix, shared-harness fake prompt/stream/completion/error matrix, daemon first-turn route test, daemon scheduled-follow-up/stale-managed-session route tests, daemon live-harness interrupt/cancel route test, `LiveDriveTests/testGrokLiveDrive` gated. | Not run; account validity not inspected. |

## Model Path Matrix

Unless a row calls out narrower evidence, `test_workspaceDraftFirstSendPlanCoversEveryBundledCodeProviderModel` covers its draft first-send planning: selected provider/model/worktree preservation, unsupported-effort clearing, `userComposerFirstTurn`, `sendAsFollowUp == false`, source-id preservation, and prompt-guard acceptance.

| Provider | Model path | Thinking | Effort | Context | Credential status | Actual observed behavior | Mock/source coverage | Live smoke |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Claude | `claude-fable-5-1m` | Yes | Yes | 1,000,000 | Subscription (Claude Code PTY) | Added 2026-06-10: bundled selectable model when Claude enabled (Chat + Code pickers); first catalog row, so it is the catalog-derived default. AgentSpawner translates the `-1m` suffix to the CLI's `claude-fable-5[1m]` form at spawn. Launch request preserves model/effort in the catalog sweep; rendered composer matrix covers it. Pricing: manual override in `pricing-overrides.json` + `pricing.json` ($10/M in, $50/M out, $1/M cache read); analytics cache bumped to v18 so previously-$0 cached Fable days re-price. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `test_workspaceDraftFirstSendPlanCoversEveryBundledCodeProviderModel`; `ComposerSendStopRenderingTests` matrix; `PricingTests/test_fable5_pricesAtLaunchRatesIncludingBracketVariant`. | The user's 3,826 real `claude-fable-5` + 139 `claude-fable-5[1m]` JSONL rows are the live evidence the CLI path works. |
| Claude | `claude-fable-5` | Yes | Yes | 200,000 | Subscription (Claude Code PTY) | Added 2026-06-10: bundled selectable model when Claude enabled; plain id passed to the CLI verbatim (no alias — unverified aliases are not wired). Same pricing override; `claude-fable-5[1m]` usage strings resolve to the same rates via prefix matching. | Same catalog-sweep + pricing coverage as the 1M row. | Same JSONL evidence as above. |
| Claude | `claude-opus-4-8-1m` | Yes | Yes | 1,000,000 | Not inspected | Bundled selectable model when Claude enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Claude | `claude-opus-4-8` | Yes | Yes | 200,000 | Not inspected | Bundled selectable model when Claude enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Claude | `claude-opus-4-7-1m` | Yes | Yes | 1,000,000 | Not inspected | Bundled selectable model when Claude enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Claude | `claude-opus-4-7` | Yes | Yes | 200,000 | Not inspected | Bundled selectable model when Claude enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Claude | `claude-opus-4-6-1m` | Yes | Yes | 1,000,000 | Not inspected | Bundled selectable model when Claude enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Claude | `claude-sonnet-4-6` | Yes | Yes | 200,000 | Not inspected | Bundled selectable model when Claude enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Claude | `claude-haiku-4-5-20251001` | No | No | 200,000 | Not inspected | Effort must be cleared when selected; launch request preserves nil effort in catalog sweep. Rendered picker coverage verifies search finds this row, favorite toggling persists it into the Favorites rail, selecting it clears stale Minimal effort from the draft chip, and the active tab remains a draft. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `testDraftModelPickerSearchFavoriteShortcutAndUnsupportedEffortPaths`. | Not run. |
| Codex | `gpt-5.5` | No | Yes | Unknown | Not inspected | Used by quick spawn/default matrix; launch request preserves selected model and `.high` effort. Daemon first-turn route coverage verifies `userComposerFirstTurn` with `asFollowUp == false` reaches a fake live Codex app-server bridge. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `test_firstTurnSendOriginRoutesThroughDaemonForEverySharedHarnessProvider`; gated live Codex test exists. | Not run. |
| Codex | `gpt-5.4` | No | Yes | Unknown | Not inspected | Bundled selectable model when Codex enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Codex | `gpt-5.3-codex-spark` | No | Yes | Unknown | Not inspected | Bundled selectable model when Codex enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Codex | `gpt-5.3-codex` | No | Yes | Unknown | Not inspected | Bundled selectable model when Codex enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Codex | `gpt-5.2-codex` | No | Yes | Unknown | Not inspected | Bundled selectable model when Codex enabled; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Gemini/Antigravity | `gemini-3.5-flash` | No | No | 1,000,000 | Not inspected | Bundled default for Antigravity; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; Antigravity state reader tests. | Not run. |
| Gemini/Antigravity | `gemini-3.5-flash-thinking` | Yes | No | 1,000,000 | Not inspected | Launch request preserves selected Antigravity thinking model and nil effort. Same-worktree draft first-send planning preserves this selected model, clears stale effort inherited from Claude, targets the existing worktree, and sends as first turn rather than follow-up; the broader planner matrix repeats the same first-turn guarantees for every bundled provider/model. Rendered UI coverage verifies selecting this row from a draft updates the draft chip to Gemini 3.5 Flash and keeps the active tab on the draft instead of opening a stale session. Daemon first-turn route coverage verifies `userComposerFirstTurn` with `asFollowUp == false` reaches a fake live Gemini/Antigravity harness. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `test_workspaceDraftFirstSendPlanUsesSelectedDraftModelAsFirstTurn`; `test_workspaceDraftFirstSendPlanCoversEveryBundledCodeProviderModel`; `test_workspaceDraftModelPickerSelectionSurfacesConfigurationWithin100ms`; `testDraftModelPickerSwitchesDraftToAntigravityWithoutOpeningOldSession`; `testDraftModelPickerSearchFavoriteShortcutAndUnsupportedEffortPaths`; `test_firstTurnSendOriginRoutesThroughDaemonForEverySharedHarnessProvider`; gated live Antigravity test exists. | Not run. |
| Gemini/Antigravity | `gemini-3-pro` | Yes | No | 2,000,000 | Not inspected | Bundled selectable model when Gemini enabled; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Gemini/Antigravity | `gemini-3.1-pro-high` | Yes | No | 1,000,000 | Not inspected | Bundled selectable model when Gemini enabled; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; Antigravity state reader coverage is adjacent. | Not run. |
| Gemini/Antigravity | `gemini-3.1-pro-low` | No | No | 1,000,000 | Not inspected | Bundled selectable model when Gemini enabled; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; Antigravity state reader coverage is adjacent. | Not run. |
| Gemini/Antigravity | `gemini-3-flash` | No | No | 1,000,000 | Not inspected | Bundled selectable model when Gemini enabled; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| Gemini/Antigravity | `gemini-3-flash-thinking` | Yes | No | 1,000,000 | Not inspected | Bundled selectable model when Gemini enabled; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`. | Not run. |
| OpenCode/OpenRouter | `openai/gpt-5.5` | Yes | Yes | Unknown | Not inspected | Fallback selectable model when OpenCode ready; launch request preserves model and `.high` effort; live probe replacement coverage verifies stale fallback selection is replaced by the live OpenRouter row and unsupported effort is cleared when the live row does not support effort. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `test_liveCatalogReplacementNormalizesStalePickerModelsAndEffort`; provider-default/source coverage. | Not run. |
| OpenCode/OpenRouter | `anthropic/claude-opus-4.7` | Yes | Yes | 200,000 | Not inspected | Fallback selectable model when OpenCode ready; launch request preserves model and `.high` effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; provider-default/source coverage. | Not run. |
| OpenCode/OpenRouter | `anthropic/claude-sonnet-4.6` | Yes | Yes | 200,000 | Not inspected | Launch request preserves selected OpenCode model and `.high` effort; send body maps this picked OpenRouter slug to OpenCode's `{providerID:"openrouter",modelID:<slug>}` object. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; provider-default tests; `OpencodeSendTests/test_requestBody_includesModelOverrideForPickedOpenRouterModel`. | Not run. |
| OpenCode/OpenRouter | `google/gemini-3-pro` | Yes | No | 2,000,000 | Not inspected | Fallback selectable model; launch request preserves nil effort in catalog sweep. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; provider-default/source coverage. | Not run. |
| OpenCode/OpenRouter | `opencode-default` | Yes | No | Unknown | Not inspected | Fallback BYOK/default model; launch request preserves nil effort in catalog sweep; send body intentionally omits the model override for this sentinel. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `OpencodeSendTests/test_requestBody_includesModelOverrideForPickedOpenRouterModel`; `LiveDriveTests/testOpenCodeLiveDrive` gated. | Not run. |
| Cursor | `cursor-default` | Yes | No | Unknown | Not inspected | Launch request preserves selected Cursor Auto model and nil effort. Live probe replacement coverage verifies stale fallback selection is replaced by the live Cursor row and unsupported effort is cleared. Daemon first-turn route coverage verifies `userComposerFirstTurn` with `asFollowUp == false` reaches a fake live Cursor harness. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; `test_liveCatalogReplacementNormalizesStalePickerModelsAndEffort`; Cursor catalog/parser tests; `test_firstTurnSendOriginRoutesThroughDaemonForEverySharedHarnessProvider`; gated live Cursor test exists. | Not run. |
| Grok | `grok-build` | Yes | Yes | 500,000 | Not inspected | Bundled fallback default; launch request preserves model and `.high` effort when Grok ready. Daemon first-turn route coverage verifies `userComposerFirstTurn` with `asFollowUp == false` reaches a fake live Grok harness. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; Grok headless/ACP support tests; `test_firstTurnSendOriginRoutesThroughDaemonForEverySharedHarnessProvider`; gated live Grok test exists. | Not run. |
| Grok | `grok-composer-2.5-fast` | No | Yes | 256,000 | Not inspected | Bundled selectable model when Grok ready; launch request preserves model and `.high` effort. | `test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath`; Grok headless/analytics source coverage. | Not run. |

## Current Evidence And Required Next Checks

- Current green evidence: Mac build, terminal regression set, unbounded rendered Code workspace tab creation via direct `+` and `Cmd+T`, rendered same-worktree session tabs for six provider/model sessions across Claude, Codex, Antigravity, OpenCode, Cursor, and Grok with center-header switch proof, tab-strip `+` source/test coverage that keeps the create-tab affordance outside the scrollable tab items, adjacent to fitted tabs, and hittable after overflow, same-worktree top-tab source coverage for six model sessions across Claude, Codex, Antigravity, Cursor, OpenCode, and Grok, compact tab-strip sizing that fits four chat tabs plus `+` at the minimum center width before scrolling, repo `+` reserved-worktree provisional row selection with stale-branch avoidance and 100 ms feedback, ANSI paste cleanup through CSI/OSC/bracketed-paste/control sequences under 100 ms, prompt-history saved/history filtering plus target IDs under 100 ms, rendered prompt-history and expanded-editor click tests, Workbench local-control 100 ms latency matrix, rendered Git Diff Stage/Unstage outcome coverage, Git diff store action/unit coverage, deterministic PR coordinator approve/create/merge action-outcome coverage, all-bundled provisional provider/model launch request matrix, dynamic OpenRouter/Cursor live-catalog replacement normalization, same-worktree draft first-send classification for Gemini/Antigravity thinking, draft first-send planner matrix for every bundled Code provider/model, connecting-empty first-turn Send/Queue/Stop classification, rendered draft picker switch to Gemini 3.5 Flash Thinking, rendered picker search/favorite/command-digit/effort-cycle/unsupported-effort coverage, rendered no-spend composer Send/Stop matrix for every bundled Code provider/model, shared-harness fake prompt/stream/completion/error matrix for Codex, Gemini/Antigravity, Cursor, and Grok, daemon first-turn `/send` routing for those shared-harness providers, daemon scheduled-follow-up/stale-managed-session route coverage for those shared-harness providers, daemon `/interrupt` live-harness cancel routing for those shared-harness providers, provider-disabled/auth-unavailable route ordering for Cursor and OpenRouter, Claude PTY JSONL fake stream projection, Claude Code terminal WS direct-shell routing, Claude Code approve-plan route restart/progress regression, OpenCode serve SSE fake prompt/reply/error/lifecycle projection, OpenCode selected-model send-body mapping, OpenCode explicit unsupported interrupt/local Send-Stop restore, latest direct `ChatSendPerProviderTests` pass, latest direct `AgentControlServerChatRouteTests` pass with live-provider skips only, and a gated OpenCode live-drive test that compiles and skips without the live-spend marker.
- Current missing evidence: live UI send/stop smoke results where credentials are present.
- Current runner caveat: local app-hosted unit execution uses direct `xcrun xctest` after `build-for-testing` because `ClawdmeterMacTests` carries the needed app-host rpaths. The reproducible command is `script/run_code_tab_unit_checks.sh`; it passed on 2026-06-09 at 21:31 ICT in `/tmp/clawdmeter-code-tab-unit-checks-composer-history-expanded.log` with `SessionLauncherModelTests` 20/20, `WorkspaceTabsTests` 36/36, `WorkbenchStateTests` 20/20, `ChatSendPerProviderTests` 15/15, and `AgentControlServerChatRouteTests` 35 executed with 4 live-provider skips and 0 failures. That direct path covers the repo `+`, tab, terminal, first-send, model-picker, provider send, daemon route, plan approval route, workbench local-state latency, provider-unavailable, compact tab-strip, ANSI paste, prompt history, and JSONL first-message tailing regressions. The rendered Plan approval click regression compiles in `/tmp/clawdmeter-ui-plan-pane-approve-build-for-testing.log`. Focused UI execution is now green for the extended `+` overflow path in `/tmp/clawdmeter-ui-tabcap-newtab.log`, repeated `Cmd+T` in `/tmp/clawdmeter-ui-tabcap-shortcut.log`, rendered same-worktree session tabs in `/tmp/clawdmeter-ui-same-worktree-session-tabs.log`, and prompt-history plus expanded-editor sheets in `/tmp/clawdmeter-ui-composer-sheets.log`.
- Latest broad direct unit rerun: `script/run_code_tab_unit_checks.sh` passed again on 2026-06-09 at 22:15 ICT in `/tmp/clawdmeter-code-tab-unit-checks-final.log` with the same five focused Code-tab classes green: `SessionLauncherModelTests` 20/20, `WorkspaceTabsTests` 36/36, `WorkbenchStateTests` 20/20, `ChatSendPerProviderTests` 15/15, and `AgentControlServerChatRouteTests` 35 executed with 4 expected live-provider skips.
- Unsupported capabilities must remain visible as disabled/omitted paths rather than silently falling back to a different provider/model.
- Effort support is the first concrete unsupported-feature rule already covered by the provisional launch test for nil-effort providers, draft Antigravity picker selection, and rendered Claude Haiku unsupported-effort picker selection.
