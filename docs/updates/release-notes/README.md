# Release Notes

Add one Markdown file per Mac release before running `tools/release-mac.sh`:

`docs/updates/release-notes/<version>.md`

The release script refuses to publish an appcast without a matching notes file.
