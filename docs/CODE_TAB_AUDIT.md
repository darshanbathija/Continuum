# Code Tab Baseline Audit

Date: 2026-06-09 04:02 ICT

Latest evidence update: 2026-06-09 22:33 ICT.

Scope: primary navigation `Code` tab and the in-tab workbench. This excludes Settings, Usage, and menu-bar surfaces except where a Code-tab control intentionally opens or depends on them.

Important baseline note: this audit was requested after Code-tab WIP was already present in the working tree. It is therefore a current-state baseline for commit `ea54eb70e6b1e39789e70f1c79ffe2faf5d17c72` plus dirty tree, not a clean pre-edit snapshot.

## Repository State

- Branch: `codex/fix-code-sidebar-usage-refresh`
- Commit: `ea54eb70e6b1e39789e70f1c79ffe2faf5d17c72`
- PR source refresh: `CODE_TAB_INTENT.md` now records a reproducible GitHub refresh from `gh pr list --repo darshanbathija/Continuum --state merged --limit 300 --json number,title,mergedAt,files,url`. The current newest 150 Code-tab-matching merged PRs run from #281 through #90; the generated evidence cache for this run is `/tmp/continuum_merged_prs_300.json`.
- Dirty tree at capture time: 27 modified tracked files and 5 pre-existing untracked docs. After this audit pass, add `docs/CODE_TAB_AUDIT.md` and `docs/CODE_TAB_PROVIDER_MATRIX.md` to the untracked docs list.
- Modified tracked areas:
  - Xcode schemes for Mac UI Tests, Mac, Watch, and iOS.
  - Code-tab/runtime files: `SessionsView.swift`, `SidebarPane.swift`, `WorkspaceTabStrip.swift`, `SessionWorkspaceView.swift`, `CenterThread.swift`, `WorkspaceReviewPane.swift`, `WorkspaceTerminalPane.swift`, `TerminalTabContainer.swift`, `WorkbenchState.swift`, `ComposerInputCore.swift`, `UsageStatusChip.swift`, `InAppBrowser.swift`, `ProvisioningTrailView.swift`, `ReviewPaneGutter.swift`, `MacRootView.swift`.
  - Agent/runtime files: `AgentControlServer.swift`, `AgentSessionRegistry.swift`, `AppDelegate.swift`.
  - Test files: `SessionLauncherModelTests.swift`, `WorkspaceTabsTests.swift`, `AgentControlServerChatRouteTests.swift`, `AgentSessionRegistryEventStoreWireTests.swift`, `CodeTabHoverShortcutUITests.swift`.
- Untracked docs: `CODE_TAB_INTENT.md`, `docs/CODE-TAB-RELEASE-AUDIT-2026-06-09.md`, `docs/BUG-AUDIT-2026-06-05.md`, `docs/BUG-AUDIT-2026-06-06-main.md`, `docs/BUG-AUDIT-2026-06-06.md`, `docs/CODE_TAB_AUDIT.md`, `docs/CODE_TAB_PROVIDER_MATRIX.md`.

## Build, Lint, And Test Status

- Project discovery: `apple/Clawdmeter.xcodeproj` with schemes `Clawdmeter (Mac)`, `Clawdmeter (Mac UI Tests)`, `Clawdmeter (iOS)`, `Clawdmeter (Watch)`, and `ClawdmeterShared`.
- Build: `xcodebuild build -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac)" -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` passed.
- Latest build diagnostics pass: the same Mac build command passed with no filtered `error:`, `warning:`, actor-isolation, SwiftUI background-publish, Thread Performance Checker, SQLite vnode, or ProviderDefaults diagnostics in `/tmp/clawdmeter-build-final-terminal-fix.log`.
- Terminal regression tests: selected XCTest tests passed. The latest 6-test terminal-focused run was:
  - `WorkspaceTabsTests/test_openOrCreateWorkspaceTerminalTabSurfacesPendingTabWithin100ms`
  - `WorkspaceTabsTests/test_openWorkspaceTerminalTabAllowsHarnessSessionDirectWorktreeShell`
  - `WorkspaceTabsTests/test_openWorkspaceTerminalTabRejectsLegacyPaneBackedSessions`
  - `TerminalPtyHostTests/test_directPtyHostStreamsInputOutputAndAppliesResize`
  - `TerminalPtyHostTests/test_terminalRegistryDropsHostAfterNaturalExit`
  - `TerminalPtyHostTests/test_terminalKillTerminatesProcessGroup`
- Prior route coverage also passed for `AgentControlServerChatRouteTests/test_terminalAddOnHarnessSessionCreatesDirectWorktreePane`.
- Terminal unbounded-tab regression: `WorkspaceTabsTests/test_openOrCreateWorkspaceTerminalTabAppendsBeyondSevenWithoutPendingReuse` and `AgentControlServerChatRouteTests/test_addDirectTerminalPaneRouteAllowsMoreThanSevenQuickTabs` passed on 2026-06-09 at 11:55 ICT in `/tmp/clawdmeter-unbounded-terminal-tabs-rerun.log`. This proves eight repeated Code workspace terminal-tab requests append visible tabs within 250 ms, promote to direct-shell panes, and the daemon `/sessions/:id/terminals` route accepts eight quick panes without the old swap throttle or seven-pane cap. Earlier evidence also exists in `/tmp/clawdmeter-terminal-tabs-unbounded.log`, with neighbor terminal/draft reruns in `/tmp/clawdmeter-workspace-tabs-neighbor.log`.
- Same-worktree session-tab cap regression: `WorkspaceTabsTests/test_sameWorkspaceModelSessionTabsAreNotCappedAtTwo` and `WorkspaceTabsTests/test_workspaceTabStripCompactsBeyondTwoTabsOnMinimumCenterWidth` passed on 2026-06-09 at 20:33 ICT in `/tmp/clawdmeter-code-tab-unit-checks-tabcap.log`. The source test creates six sessions in one worktree across Claude, Codex, Antigravity, Cursor, OpenCode, and Grok, then verifies the top-tab source includes all six session IDs rather than trimming the list to two. The strip-sizing test verifies four chat tabs plus `+` fit at the 420 pt minimum center-pane width, and larger counts keep a compact scrollable width instead of shrinking to zero or dropping tabs. The rendered macOS UI regression `CodeTabHoverShortcutUITests/testWorkspaceSessionTabsRenderEverySameWorktreeModelSessionAndSwitchHeader` passed on 2026-06-09 at 22:33 ICT in `/tmp/clawdmeter-ui-same-worktree-session-tabs.log`, proving six same-worktree provider/model session tabs render and switching crowded Cursor/Grok tabs updates the center header to the selected session/provider/model.
- Terminal reconnect-state regression: `TerminalWebSocketChannelTests/test_connectionStatePresentationSurfacesReconnectAndFailure`, `test_macTerminalCoordinatorPublishesReconnectStateForReadFailure`, and `test_outputFramesIncludeInitialClearAndPtyBytes` passed on 2026-06-09 at 12:32 ICT in `/tmp/clawdmeter-terminal-reconnect-state-unit.log`. This proves the terminal coordinator publishes a reconnecting state on read failure and the shared presentation maps connected/reconnecting/failed states to visible Code-tab status text.
- Terminal live socket-drop regression: `CodeTabHoverShortcutUITests/testWorkspaceTerminalReconnectsAfterSocketDrop` passed on 2026-06-09 at 13:08 ICT in the combined `/tmp/clawdmeter-ui-tabs-terminal-socket-drop-final.log` run. The rendered workspace terminal reached `Terminal connected`, a UI-test-only distributed notification forced the terminal WebSocket down, the UI surfaced `Terminal reconnecting`, and it recovered to `Terminal connected`.
- Plan-pane approval regression: `AgentControlServerChatRouteTests/test_approvePlanRouteForClaudeCodeMarksPlanApprovedAndRuns` passed on 2026-06-09 at 20:21 ICT in `/tmp/clawdmeter-code-tab-unit-checks-with-workbench-latency.log`. It posts `/sessions/:id/approve-plan` for a planning Claude Code session, clears `planText`, persists the approved plan, seeds progress, moves the session to running, and spawns a fake Claude PTY. A rendered UI regression, `CodeTabHoverShortcutUITests/testPlanPaneApproveButtonApprovesRenderedPendingPlanAndCreatesCheckpoint`, now compiles in `/tmp/clawdmeter-ui-plan-pane-approve-build-for-testing.log`; the full click run in `/tmp/clawdmeter-ui-plan-pane-approve.log` was intentionally stopped because the Mac was at the lock screen, so rendered pending -> approved visual evidence is still pending on an unlocked desktop.
- Replay/event-store regression: `AgentSessionRegistryEventStoreWireTests` passed 11 tests, including `test_replaySeedDoesNotBlockRegistryInitOnMainActor`.
- Code-tab UI runner coverage: `ClawdmeterMacUITests/CodeTabHoverShortcutUITests` previously passed the 7-test class run covering composer controls, sidebar controls, new-session shortcut, rename shortcut, preview-chip-to-browser, review-pane controls, and the workspace terminal shortcut. The class now contains Code-only tests for Terminal review-pane shortcut coverage, titlebar/Cmd-K search coverage, titlebar right-pane menu row coverage, center-header density/more-actions menu coverage, center-header archive/end behavior coverage, repo-settings menu row coverage, repo-settings destructive behavior coverage, hover archive, unbounded tab-plus draft coverage, unbounded `Cmd+T` draft coverage, rendered draft model-picker switching, permission-mode quick flip/shortcut coverage, first-prompt queue absence, picker search/favorite/shortcut/effort-cycle paths, rendered prompt-history sheet search/use/copy/delete, and rendered expanded-editor save/Done/menu-reuse coverage. The latest focused center-header destructive rerun on 2026-06-09 at 09:49 ICT passed `testCenterHeaderMoreActionsArchiveHidesSessionAndKeepsArchiveRecoverable` and `testCenterHeaderMoreActionsEndSessionDeletesNonOwnedSession` in `/tmp/clawdmeter-ui-center-header-destructive.log`; the tests verify Archive hides the active session while keeping it recoverable in the Archive bucket, and End Session deletes the non-owned fixture session without removing the managed workspace record or surfacing the deleted row under Archive. The latest focused repo-settings destructive rerun on 2026-06-09 at 09:44 ICT passed `testRepoSettingsArchiveAllArchivesSessionsWithoutRemovingWorkspace`, `testRepoSettingsArchiveEntireRepoArchivesSessionsAndRemovesWorkspace`, and `testRepoSettingsRemoveDropsWorkspaceRecordWithoutArchivingSession` in `/tmp/clawdmeter-ui-repo-settings-destructive.log`; the tests verify Archive All preserves the workspace record while archiving sessions, Archive Entire Repo archives sessions and removes the workspace record while leaving archived sessions recoverable, and Remove drops only the workspace record without archiving/deleting session history. The latest focused repo settings rerun on 2026-06-09 at 09:39 ICT passed `testRepoSettingsMenuExposesRowsAndNewSessionAction` in `/tmp/clawdmeter-ui-repo-settings-menu.log`; the test verified New Session, Archive All, Archive Repo, Settings, and Remove rows by stable IDs, then clicked the non-destructive New Session row and observed the Code launcher Start action. The latest focused center-header menu rerun on 2026-06-09 at 09:29 ICT passed `testCenterHeaderDensityAndMoreActionsMenusExposeSafeActions` in `/tmp/clawdmeter-ui-center-header-menus.log`; the test verified density selected-state changes, safe More-actions rows, schedule sheet open/dismiss, and the Terminal row opening `code.workspace.tab.terminal` plus `code.workspace.terminal.surface`. The latest focused titlebar right-pane menu rerun on 2026-06-09 at 09:22 ICT passed `testTitlebarRightPaneMenuSelectsEveryPaneAndCollapse` in `/tmp/clawdmeter-ui-titlebar-right-pane-menu.log`; the test clicked Plan, Diff, Terminal, Sources, Artifacts, PR, Browser, and Collapse through stable menu-item IDs and verified each selected-pane marker or collapsed state. The latest focused first-prompt UI rerun on 2026-06-09 at 09:14 ICT passed `testDraftFirstPromptComposerDoesNotExposeQueuedFollowUpsBeforeSend` in `/tmp/clawdmeter-ui-first-prompt-not-queued.log`. The latest focused permission-mode rerun on 2026-06-09 at 08:54 ICT passed `testPermissionModeChipQuickFlipAndShortcutsUpdateDraftComposer` in `/tmp/clawdmeter-ui-permission-mode-chip-rerun.log`; an earlier run of that same test failed because `Menu(primaryAction:)` exposed the chip as a popup and opened the menu instead of quick-flipping, which is now fixed by an explicit primary button plus chevron menu. The latest focused `Cmd+T` rerun on 2026-06-09 at 08:45 ICT passed `testWorkspaceNewTabShortcutCreatesUnboundedChatDraftTabsFromDraftSelection` in `/tmp/clawdmeter-ui-workspace-tabs-shortcut-unbounded.log`. The latest focused direct tab-plus rerun on 2026-06-09 at 08:42 ICT passed `testWorkspaceNewTabButtonCreatesUnboundedChatDraftTabsFromCodeTab` in `/tmp/clawdmeter-ui-workspace-tabs-unbounded.log`. The latest focused Terminal/right-pane 2-test rerun passed in `/tmp/clawdmeter-ui-review-pane-terminal-accessibility.log`. The earlier focused tab/effort UI rerun on 2026-06-09 at 07:49 ICT passed that same unbounded-tab test plus `testDraftModelPickerSearchFavoriteShortcutAndUnsupportedEffortPaths` in `/tmp/clawdmeter-ui-tabs-effort-final.log`. This proves repeated direct `+` clicks append five draft tabs, repeated `Cmd+T` appends draft tabs while a draft is already selected, repo settings exposes stable menu rows and performs repo-scoped new-session/archive/remove actions without losing recoverable session data, center-header Archive and End Session produce the documented persistent state, the center-header density menu exposes selected state and updates, safe More-actions rows route to schedule and terminal surfaces, the titlebar right-pane menu selects every pane and collapses the pane, a typed first prompt in a draft enables the icon-only send target without exposing the queued follow-up panel, permission-chip clicks quick-flip Ask -> Plan -> Accept edits on a draft, `Cmd+Shift+1/2` switch Ask/Accept edits without opening a stale session, `Cmd+1` in the picker no longer triggers global Chat navigation, picker effort selection updates the chip, search finds Claude Haiku, favorites persist into the Favorites rail, `Cmd+Opt+E` cycles Minimal to Low, `Cmd+Opt+Shift+E` cycles Low back to Minimal, and unsupported-effort Haiku clears stale effort. The latest broader picker/tab rerun in `/tmp/clawdmeter-ui-tabs-picker-combined.log` also proves rendered picker selection can switch a draft to Antigravity Gemini 3.5 Flash Thinking and keep the active workspace on the draft tab. The latest focused terminal shortcut rerun on 2026-06-09 at 07:36 ICT passed in `/tmp/clawdmeter-ui-code-terminal-focused.log`, showing `Shift+Cmd+T` creates `code.workspace.tab.terminal`, renders `code.workspace.terminal.surface`, and reaches `Terminal connected`. The newest composer-history/expanded-editor UI-test target build passed in `/tmp/clawdmeter-ui-composer-history-expanded-build.log`; focused execution attempts in `/tmp/clawdmeter-ui-composer-history-expanded.log` and `/tmp/clawdmeter-ui-expanded-editor.log` were interrupted after `xcodebuild test-without-building` stalled before product launch, so those rendered clicks remain execution-pending.
- Current permission-mode and tab-cap UI runner coverage: the latest focused permission-mode reruns on 2026-06-09 at 11:21-11:22 ICT passed `testPermissionModeMenuRowsAndBypassTrustSheetGate` in `/tmp/clawdmeter-ui-permission-mode-menu-bypass.log` and `testPermissionModeChipQuickFlipAndShortcutsUpdateDraftComposer` in `/tmp/clawdmeter-ui-permission-mode-quickflip.log`. The menu/bypass test verifies the native chevron menu exposes Ask, Accept edits, Plan, and Bypass rows, selecting Bypass on a bound session opens the trust sheet before mode change, Cancel preserves the prior mode, and draft menu row selections update the chip. The latest current-build unbounded-tab unit rerun passed `WorkspaceTabsTests/test_openDraftWorkspaceTabAllowsMultipleUntitledTabsPerWorkspace`, `test_openDraftWorkspaceTabFromSelectedDraftAppendsUnboundedSiblingDrafts`, `test_workspaceDraftTabActionsSurfaceVisibleStateWithin100ms`, `test_workspaceDraftModelPickerSelectionSurfacesConfigurationWithin100ms`, and `test_workspaceTabStripCompactsBeyondTwoTabsOnMinimumCenterWidth` on 2026-06-09 at 21:13 ICT in `/tmp/clawdmeter-code-tab-unit-checks-refined-plus.log`. The latest current-build unbounded-tab UI rerun passed `testWorkspaceNewTabButtonCreatesUnboundedChatDraftTabsFromCodeTab` on 2026-06-09 at 16:27 ICT in `/tmp/clawdmeter-ui-terminal-tabcap-current.log`, proving repeated direct `+` clicks append draft tabs instead of capping at two. The current source keeps the `+` button outside the scrollable tab items, sizes the scroll region so `+` stays adjacent when tabs fit, and reserves button width when overflowed tabs scroll; `/tmp/clawdmeter-ui-refined-plus-build-for-testing.log` compiles the extended UI assertion requiring `+` to stay hittable after overflow and append a sixth draft. The previous 16:20 ICT run in `/tmp/clawdmeter-ui-workspace-tab-unbounded.log` also passed repeated `Cmd+T` draft-tab creation. Earlier terminal-paired evidence is also retained in `/tmp/clawdmeter-ui-tabs-terminal-socket-drop-final.log`, `/tmp/clawdmeter-ui-unbounded-tabs-after-terminal-teardown-fix.log`, `/tmp/clawdmeter-ui-unbounded-chat-tabs.log`, and `/tmp/clawdmeter-ui-workspace-tabs-unbounded-current.log`.
- Latest current-build unbounded rendered tab rerun: `CodeTabHoverShortcutUITests/testWorkspaceNewTabButtonCreatesUnboundedChatDraftTabsFromCodeTab` passed on 2026-06-09 in `/tmp/clawdmeter-ui-tabcap-newtab.log`, and `testWorkspaceNewTabShortcutCreatesUnboundedChatDraftTabsFromDraftSelection` passed in `/tmp/clawdmeter-ui-tabcap-shortcut.log`. The direct `+` path now proves six rendered draft tabs, the selected fifth tab remains hittable after auto-scroll, and the `+` affordance remains hittable after overflow; the `Cmd+T` path proves repeated draft creation while a draft is already selected.
- Latest same-worktree rendered session-tab rerun: `CodeTabHoverShortcutUITests/testWorkspaceSessionTabsRenderEverySameWorktreeModelSessionAndSwitchHeader` passed on 2026-06-09 in `/tmp/clawdmeter-ui-same-worktree-session-tabs.log`. It seeds six model sessions in one worktree across Claude, Codex, Antigravity, OpenCode, Cursor, and Grok, verifies every session tab is exposed by stable id/provider/model value, and proves switching crowded Cursor and Grok tabs updates the center header state to the selected session/provider/model instead of capping or sticking on the old session.
- Latest current-build rendered composer sheet rerun: `CodeTabHoverShortcutUITests/testExpandedComposerEditorSavesPromptAndSavedPromptMenuReusesIt` and `testPromptHistoryRenderedSheetSearchUseCopyAndDeleteRows` passed together on 2026-06-09 in `/tmp/clawdmeter-ui-composer-sheets.log` with result bundle `/tmp/clawdmeter-ui-composer-sheets.xcresult`. This supersedes the older build-only note for prompt history and expanded-editor clicks.
- New-tab secondary Terminal UI runner coverage: the latest focused rerun on 2026-06-09 at 11:44 ICT passed `CodeTabHoverShortcutUITests/testWorkspaceNewTabContextMenuTerminalOpensDirectShellTab` in `/tmp/clawdmeter-ui-tab-plus-terminal-context.log`. It proves the workspace tab-strip `+` context menu exposes Chat and Terminal rows with stable targets, and that the Terminal row creates `code.workspace.tab.terminal`, renders `code.workspace.terminal.surface`, and shows explicit Terminal connection feedback.
- Rendered Diff/PR pane UI runner coverage: the latest focused rerun on 2026-06-09 at 15:54 ICT passed `CodeTabHoverShortcutUITests/testDiffReviewPaneRenderedControlsClickThroughSeededGitDiff` and `testPRReviewPaneLoadedFixtureSafeActionsClickThrough` in `/tmp/clawdmeter-rendered-diff-pr-ui-current.log`. The Diff test opens a seeded dirty worktree, verifies toolbar/file/hunk targets, clicks layout, Next unviewed, Mark all viewed, hunk collapse/expand, and Explain, and verifies Explain inserts a composer prompt. The latest rendered Git action rerun on 2026-06-09 at 16:19 ICT passed `CodeTabHoverShortcutUITests/testGitDiffPaneRenderedStageUnstageButtonsMutateSeededRepo` in `/tmp/clawdmeter-ui-git-diff-stage-unstage.log`; it switches the right Diff pane into Git mode, verifies the pane loads `files:2 staged:1 unstaged:1`, clicks rendered Stage, verifies `git status` becomes `M  notes.md`, clicks rendered Unstage, verifies `git status` becomes ` M notes.md`, and confirms Stage is exposed again. The PR test opens a loaded fixture, verifies title/subtitle/status/check/action targets, clicks Request changes to insert a composer prompt, and clicks Copy URL, Copy Number, and Ask agent to fix checks through the compact action menu.
- Rendered context-usage popover UI runner coverage: the latest focused rerun on 2026-06-09 at 16:00 ICT passed `CodeTabHoverShortcutUITests/testContextUsageChipOpensRenderedPopoverRowsFromCodeComposer` in `/tmp/clawdmeter-ui-context-usage-popover.log`. It clicks the rendered Code composer `code.composer.context-usage` chip, verifies the ring value remains context-window based, then verifies the popover plus `code.context-usage.row.context` and `code.context-usage.row.cost` rows render from the descriptor-backed targets. The descriptor rerun passed again on 2026-06-09 at 16:02 ICT in `/tmp/clawdmeter-context-usage-descriptor-current.log`.
- Active worktree selection regression: `WorkspaceTabsTests/test_activeWorkspaceKeyTracksOneSelectedWorktreeAcrossTabTypes` passed on 2026-06-09 at 11:58 ICT in `/tmp/clawdmeter-active-workspace-selection.log`. It creates two worktrees and proves session, draft, terminal, and document tab selections resolve to exactly one active workspace key. The adjacent rendered draft/model-switch smoke, `CodeTabHoverShortcutUITests/testDraftModelPickerSwitchesDraftToAntigravityWithoutOpeningOldSession`, passed on 2026-06-09 at 11:59 ICT in `/tmp/clawdmeter-ui-draft-selection-model-switch.log`, proving the Antigravity draft switch still stays on the draft tab after the sidebar selection change.
- Center-header checkpoint UI runner coverage: the latest focused checkpoint rerun on 2026-06-09 at 10:03 ICT passed `testCenterHeaderCheckpointCreateAndRestoreRoundTripsRepoState` in `/tmp/clawdmeter-ui-center-header-checkpoint.log`. The test seeds an isolated temp git repo, creates a manual checkpoint from the header menu, verifies a checkpoint ref, header feedback, and the checkpoint strip, commits a later tracked-file state, opens Restore latest checkpoint from the header menu, verifies the restore preview sheet, clicks Restore to checkpoint, and verifies the tracked file returns to the checkpoint contents. The blocked dirty-tracked rerun on 2026-06-09 at 10:11 ICT passed `testCenterHeaderCheckpointRestoreBlocksDirtyTrackedChangesAndCancelPreservesWorktree` in `/tmp/clawdmeter-ui-center-header-checkpoint-blocked.log`; it verifies dirty tracked changes surface a blocking reason and dirty-status cue, Restore is disabled, Cancel dismisses the sheet, and the dirty file contents plus `git status --porcelain` remain unchanged. The latest untracked-overwrite rerun on 2026-06-09 at 10:26 ICT passed `testCenterHeaderCheckpointRestoreBlocksUntrackedOverwriteAndCancelPreservesFile` in `/tmp/clawdmeter-ui-center-header-checkpoint-untracked-blocked.log`; it verifies a checkpoint whose target tracks `conflict.txt` blocks restore when the current worktree has an untracked `conflict.txt`, keeps Restore disabled, and Cancel preserves the untracked file plus status.
- Checkpoint safety unit coverage: `WorkbenchStateTests/test_checkpointRestoreRevalidatesUntrackedOverwriteRiskAfterPreview` and `WorkbenchStateTests/test_latestCheckpointIgnoresSafetyRestoreSnapshots` passed on 2026-06-09 at 10:25 ICT in `/tmp/clawdmeter-unit-checkpoint-untracked-safety.log`. They prove restore revalidates untracked overwrite risk after preview, preserves the untracked file when the risk appears between preview and restore, and keeps internal safety restore snapshots from replacing the user-visible latest checkpoint.
- Workspace draft-tab regressions: `WorkspaceTabsTests/test_openDraftWorkspaceTabAllowsMultipleUntitledTabsPerWorkspace`, `WorkspaceTabsTests/test_workspaceDraftComposerStoresStayIsolatedAcrossDraftTabs`, `WorkspaceTabsTests/test_workspaceDraftFirstSendPlanUsesSelectedDraftModelAsFirstTurn`, `WorkspaceTabsTests/test_workspaceDraftFirstSendPlanCoversEveryBundledCodeProviderModel`, `WorkspaceTabsTests/test_workspaceDraftTabActionsSurfaceVisibleStateWithin100ms`, and `WorkspaceTabsTests/test_workspaceDraftModelPickerSelectionSurfacesConfigurationWithin100ms` passed across focused runs. The all-bundled draft first-send planner matrix passed again on 2026-06-09 at 13:47 ICT in `/tmp/clawdmeter-composer-draft-matrix-combined.log`; the earlier combined draft/model-picker pass is in `/tmp/clawdmeter-unit-draft-tabs-model-picker-combined.log`. A focused 4-test rerun of the unbounded-tab and draft-picker latency gates passed on 2026-06-09 at 16:19 ICT in `/tmp/clawdmeter-workspace-tabs-focused.log`. They prove repeated draft opens append tabs from both a selected session and an already selected draft, preserve selection, close one draft without removing the rest, keep draft composer text/model/effort state isolated per untitled tab, route every bundled provider/model selected on a draft as first-turn same-worktree sends with unsupported effort cleared, and keep 50-draft open/select/close plus 49 model-picker selection feedback under the 100 ms budget.
- Permission-mode unit regressions: `WorkspaceTabsTests/test_permissionModeShortcutsStayStableWhenProviderHidesPlanMode` and `WorkspaceTabsTests/test_permissionModeQuickFlipUsesPlanAndAcceptEditsOnlyWhenAvailable` passed in `/tmp/clawdmeter-permission-mode-unit-final.log` on 2026-06-09 at 08:55 ICT. They prove Cursor's hidden Plan mode is ignored, Bypass remains the stable slot 4 instead of shifting to slot 3, and the quick-flip policy only targets Plan/Accept edits when available.
- Command/terminal shortcut routing regressions: the full `WorkspaceTabsTests` class passed 29 tests in `/tmp/clawdmeter-workspace-tabs-command-routing.log` on 2026-06-09 at 09:06 ICT. The new `test_rootCommandRoutingRegistersCodeTabShortcutBackedActions` proves the root command registry descriptors and workspace notifications are present for `code.newChatTab`, `code.newTerminalTab`, and `session.rename`; `test_newWorkspaceTerminalTabCanUseSiblingSourceWhenDraftIsSelected` proves terminal creation can source an eligible same-workspace session while the selected foreground tab is an unsent draft.
- Draft first-send regression: `WorkspaceTabsTests/test_workspaceDraftFirstSendPlanUsesSelectedDraftModelAsFirstTurn` passed in `/tmp/clawdmeter-unit-code-tab-archive-tabs-launcher-provider-firstsend.log` on 2026-06-09 at 06:35 ICT, and `WorkspaceTabsTests/test_workspaceDraftFirstSendPlanCoversEveryBundledCodeProviderModel` passed again in `/tmp/clawdmeter-composer-draft-matrix-combined.log` on 2026-06-09 at 13:47 ICT. Together they prove same-worktree drafts keep the selected provider/model/worktree, clear unsupported stale effort, preserve source IDs, classify the first message as `userComposerFirstTurn` with `asFollowUp == false`, and pass the provider-prompt guard across every bundled Code provider/model.
- Provider/model launch matrix regression: `SessionLauncherModelTests/test_codeTabProviderMatrixDefaultsResolveForEverySelectableAgent` and `SessionLauncherModelTests/test_provisionalLaunchFinalRequestCoversEveryBundledCodeModelPath` passed together in `/tmp/clawdmeter-provider-matrix-defaults-and-launch.log`. The launch test derives the provider/model matrix from the bundled Code catalog, covers Claude, Codex, Gemini/Antigravity, OpenCode/OpenRouter, Cursor, and Grok, verifies unique model IDs, verifies unsupported-effort paths preserve nil effort, and verifies the provisional session plus final launch request use the selected provider/model/effort instead of a stale session.
- Dynamic live-catalog replacement regression: `SessionLauncherModelTests/test_liveCatalogReplacementNormalizesStalePickerModelsAndEffort` passed in `/tmp/clawdmeter-unit-live-catalog-replacement.log` on 2026-06-09 at 06:42 ICT. It proves live OpenRouter and Cursor catalogs replace fallback picker rows and normalize stale fallback model/effort selections to the live row, clearing effort when unsupported.
- Provider send/stream fake matrix regression: `ChatSendPerProviderTests` passed 15 tests, 0 failures, in `/tmp/clawdmeter-chat-send-provider-claude-opencode.log` on 2026-06-09 at 07:56 ICT. The matrix loops Codex, Gemini/Antigravity, Cursor, and Grok through selected model/effort start parameters, first prompt delivery, streaming feedback, completion projection, and provider error projection. It also verifies Claude PTY JSONL prompt/reply/usage/lifecycle projection and OpenCode serve SSE prompt/reply/error/lifecycle projection without live provider binaries. `OpencodeSSEAdapterTests` passed 29 tests, 0 failures, in `/tmp/clawdmeter-opencode-sse-adapter-lifecycle.log` on 2026-06-09 at 07:57 ICT after the OpenCode lifecycle patch.
- Provider Stop route regression: `AgentControlServerChatRouteTests/test_interruptRoutesToLiveHarnessCancelForEverySharedHarnessProvider` passed on 2026-06-09 at 11:47 ICT in `/tmp/clawdmeter-provider-harness-interrupt-matrix.log`. It registers fake live harness bridges for Codex, Gemini/Antigravity, Cursor, and Grok, POSTs `/sessions/:id/interrupt`, and verifies the route returns 200, calls the active driver cancel path exactly once, and restores the local Send/Stop turn state to interrupted.
- Scheduled follow-up route regression: `AgentControlServerChatRouteTests/test_scheduledUserFollowUpOriginRoutesThroughDaemonForEverySharedHarnessProvider`, `test_scheduledFollowUpLegacyOriginIsBlockedBeforeProviderDelivery`, and `test_scheduledFollowUpStaleManagedSessionDoesNotFallbackToLegacyPromptPath` passed together on 2026-06-09 at 18:22 ICT with `xcodebuild test -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac)" -destination 'platform=macOS'` plus focused `-only-testing` filters. They prove scheduler-originated user follow-ups render the user row, enter streaming state, and route to Codex, Gemini/Antigravity, Cursor, and Grok live harness bridges; legacy synthetic scheduled prompts are blocked before provider delivery; and stale managed scheduled sessions return `acp_session_not_live` instead of falling back to a legacy prompt path.
- No-spend live-route gate and fixture isolation: the gated `LiveDriveTests` class passed as a compile/skip run on 2026-06-09 at 18:26 ICT: 5 tests executed, 5 skipped, 0 failures, because `CLAWDMETER_LIVE_VERIFY=1`, `CLAWDMETER_ALLOW_PROVIDER_SPEND=1`, and `~/.continuum-live-verify` were absent. The gated Code-provider route smoke filters passed on 2026-06-09 at 18:27 ICT with 6 selected tests, 3 skips, 0 failures after isolating `MobileCommandOutbox(replaysAuditLogOnStart: false)` for AgentControlServer test fixtures; logs now show `Mobile command audit replay deferred under test/no-spend gate` instead of replaying the user's real `mobile-commands.jsonl`. The Claude PTY E2E fixture now overrides `ChatCwdManager` to temp chat-session roots and a temp Claude trust file; the 2026-06-09 18:31 ICT rerun shows the pending chat JSONL under `/var/folders/.../clawdmeter-pty-e2e-.../chat-sessions/<session>/.continuum-pending-chat.jsonl`, not `~/Library/Application Support/Clawdmeter` or `~/.claude.json`.
- Composer Send/Stop rendered no-spend regression: `ComposerSendStopRenderingTests` passed 5 tests, 0 failures, again on 2026-06-09 at 13:47 ICT in `/tmp/clawdmeter-composer-draft-matrix-combined.log`. It proves idle composer state resolves to the bottom-right icon-only `code.composer.send`, running state resolves to bottom-right icon-only `code.composer.stop` plus separate queue affordance, neither action descriptor reintroduces legacy `$0.000`, `live`, or `tap to stop` text, rendered idle/running composer PNGs are nonblank and distinct at `/tmp/clawdmeter-composer/`, and every bundled Code provider/model renders nonblank idle Send and running Stop composer states with icon-only primary actions. The post-change UI runner also passed `CodeTabHoverShortcutUITests/testDraftFirstPromptComposerDoesNotExposeQueuedFollowUpsBeforeSend` on 2026-06-09 at 12:18 ICT in `/tmp/clawdmeter-ui-first-prompt-not-queued-after-send-stop.log`, proving the actual Code tab still exposes `code.composer.send` for a typed draft first prompt and does not expose the queued-followups panel before send.
- Pending retry/dismiss regression: `ComposerSendStopRenderingTests/test_pendingMessageActionsExposeRetryDismissOnlyForFailedAndOfflineStates` passed with the composer and pending-store suite on 2026-06-09 at 14:15 ICT in `/tmp/clawdmeter-composer-pending-actions.log`. It proves sending state exposes no retry/dismiss controls, failed sends expose `Retry` plus `composer.pending.dismiss`, and queued-offline sends expose `Retry now` plus `composer.pending.dismiss` with the same stable `composer.pending.retry` target. The same focused run passed `SessionChatStorePendingTests` 13 tests, proving failed pendings stay visible until user action, retry reuses the slot, dismiss clears it, offline queue capture/replay/dedupe/cap behavior is preserved, and pending-bubble injection remains under the 100 ms feedback budget.
- Plan halo/pane regression: `AgentSessionRegistryPlanProgressTests` passed 7 tests, 0 failures, on 2026-06-09 at 14:26 ICT in `/tmp/clawdmeter-plan-controls-rerun.log`. The new `test_inlinePlanHaloActionsExposeStableTargetsAndApprovalState` proves the inline plan halo exposes stable Refine, Edit, and Approve targets with approval disabled/enabled state. The new `test_reviewPlanPanePresentationPrioritizesPendingApprovalThenApprovedThenTodos` proves the right-pane Plan model prioritizes pending plans over approved plans over todos fallback, caps todos at eight rows, exposes `code.plan-pane.approve` only for pending plans, and keeps empty state action-free. The right-pane button now uses the existing `WorkspaceReviewPane.onApprove` callback, so approval routes through the workspace checkpoint-then-approve path.
- Plan approval route regression: `AgentControlServerChatRouteTests/test_approvePlanRouteForClaudeCodeMarksPlanApprovedAndRuns` passed on 2026-06-09 at 20:21 ICT in `/tmp/clawdmeter-code-tab-unit-checks-with-workbench-latency.log`. It posts `/sessions/:id/approve-plan` for a planning Claude Code session backed by a fake Claude binary, verifies HTTP 200, status `.running`, pending `planText` cleared, `approvedPlanText` persisted, plan progress seeded, and the Claude PTY host respawned in run mode. This proves the daemon side behind `code.plan-pane.approve`; the rendered right-pane test now compiles, and its click run remains pending on an unlocked desktop.
- Context usage chip regression: `ComposerSendStopRenderingTests/test_contextUsageRingUsesContextWindowOnly` and `test_contextUsagePopoverRowsUseSuppliedCodePayloadAndCursorQuota` passed on 2026-06-09 at 14:31 ICT in `/tmp/clawdmeter-context-usage-chip.log`. The ring descriptor proves the chip percentage comes only from context-window used/limit values, not 5-hour or weekly plan percentages. The popover row descriptor test proves the rows are derived from the supplied Code-session payload, expose stable `code.context-usage.row.*` targets, prefer Cursor quota rows over the generic 5-hour row when Cursor quota is present, keep weekly visible, and fall back to generic 5-hour plus weekly rows when Cursor quota is absent.
- Document-tab regression: `WorkspaceTabsTests/test_workspaceDocumentTabsOpenSelectDedupeAndCloseToOriginChat` passed on 2026-06-09 at 14:33 ICT in `/tmp/clawdmeter-document-tabs.log`, and `WorkspaceTabsTests/test_markdownDocumentPathResolutionAllowsOnlyWorktreeOrGeneratedDocsRoots` passed on 2026-06-09 at 14:33 ICT in `/tmp/clawdmeter-document-paths.log`. Together they prove Markdown document tabs normalize relative/absolute paths, select the owning worktree session, close any selected terminal tab, dedupe repeat opens, close back to the origin chat, and restrict document path resolution to the worktree or generated-doc roots.
- Diff pane descriptor regression: `WorkbenchStateTests/test_diffToolbarDescriptorExposesStableTargetsAndEnabledState` and `test_diffRowActionDescriptorsExposeStableTargets` passed on 2026-06-09 at 14:40 ICT in `/tmp/clawdmeter-diff-pane-descriptors.log`. They prove `TahoeDiffPreviewPane` exposes stable `code.diff.*` targets for the toolbar, file count, unviewed count, layout picker, next unviewed, mark all viewed, file rows, mark reviewed, flag changes, mark viewed, open, hunk rows, hunk collapse/expand, and Explain, while preserving disabled state for empty toolbars and already-viewed files.
- PR pane descriptor regression: `PRCoordinatorTests/test_prCompactPaneEmptyActionsExposeStableTargets`, `test_prCompactPaneLoadedActionsGateRerunAndMerge`, and `test_prCompactPaneStatusAndCheckDescriptorsExposeStableTargets` passed on 2026-06-09 at 14:50 ICT in `/tmp/clawdmeter-pr-pane-descriptors.log`. They prove `TahoePRCompactPane` exposes stable `code.pr.*` targets for the empty URL/load/create/draft state, PR action menu, title/subtitle, review actions, status rows, check rows, check context actions, progress, and error; the descriptors also gate create/draft by daemon availability, rerun-failed-checks by failed GitHub Actions run IDs, and merge by checks plus TODO state.
- Git diff action regression: `GitDiffStoreTests/test_gitDiffPaneActionDescriptorsExposeStableTargets` and `test_stageAndUnstageHunkUseCorrectGitDomains` passed on 2026-06-09 at 14:57 ICT in `/tmp/clawdmeter-git-diff-actions.log`. They prove `GitDiffPane` exposes stable `code.diff.git.*` targets for refresh, loading/error/clean states, file rows, file stage/revert/unstage/trash, hunk stage/revert/unstage, commit sheet open/message/cancel/submit, and commit enabled state. The temp-repo hunk test stages an unstaged hunk into the index, verifies the worktree diff clears, then unstages the staged hunk back into the worktree without losing the change.
- Credential-log redaction regression: `CredentialLoggingRedactionTests/testProviderAndRelaySourcesDoNotLogTokenDerivedMaterial` passed on 2026-06-09 at 13:57 ICT in `/tmp/clawdmeter-credential-log-redaction.log`. It source-scans Anthropic polling, Watch token bridge, app startup auto-import, live-activity APNS push, APNS gateway, and APNS device-token store paths for forbidden token-derived log snippets such as token length, token prefix, and token fingerprint. The same log shows Anthropic polling now logs only `authenticated; trying /api/oauth/usage`, and a follow-up scan found no unsafe token length/fingerprint/prefix strings in that test output.
- Public diagnostics redaction regression: `CredentialLoggingRedactionTests/testProviderAndCodeSourcesDoNotExposePromptOrResponseBodiesInPublicDiagnostics` passed with the credential scanner on 2026-06-09 at 14:06 ICT in `/tmp/clawdmeter-public-diagnostics-redaction.log`. It source-scans Anthropic, Antigravity/Gemini, Codex, Cursor, relay pairing, ask-user coordination, Tailscale whois, and Code git diff paths for prompt text, provider response body previews, trailer text, and public stdout/stderr leaks. The shared provider parser/source filter passed 31 tests, 0 failures, on 2026-06-09 at 14:07 ICT in `/tmp/clawdmeter-shared-provider-redaction.log`, including the updated Cursor trailer-only case that now requires redacted trailer size instead of trailer content. Final source and log scans found none of the forbidden public-diagnostics snippets.
- Daemon first-turn route regression: `AgentControlServerChatRouteTests/test_firstTurnSendOriginRoutesThroughDaemonForEverySharedHarnessProvider` passed in `/tmp/clawdmeter-first-turn-harness-route.log` on 2026-06-09 at 08:28 ICT. It registers fake live harness bridges for Codex, Gemini/Antigravity, Cursor, and Grok, POSTs `/sessions/:id/send` with `origin: userComposerFirstTurn` and `asFollowUp: false`, and verifies the real daemon route delivers the first prompt to the selected provider bridge, echoes the user row, and enters streaming state instead of treating the message as a queued follow-up or blocked legacy prompt. The adjacent combined route rerun passed 3 tests in `/tmp/clawdmeter-send-route-first-turn-stale-opencode.log` on 2026-06-09 at 08:29 ICT.
- OpenCode send/live-drive gate: `OpencodeSendTests` passed 11 tests, 0 failures, in `/tmp/clawdmeter-opencode-send-updated.log` on 2026-06-09 at 08:04 ICT. It now verifies picked OpenRouter model slugs map to OpenCode's `model` object while `opencode-default` omits the override. `LiveDriveTests` passed as a gated compile/no-spend run in `/tmp/clawdmeter-live-drive-gated-opencode.log` on 2026-06-09 at 08:04 ICT: 5 tests executed, 5 skipped, 0 failures, including the new `testOpenCodeLiveDrive`. The skip was expected because `CLAWDMETER_LIVE_VERIFY=1`, `CLAWDMETER_ALLOW_PROVIDER_SPEND=1`, and `~/.continuum-live-verify` were not present.
- OpenCode interrupt route: `AgentControlServerChatRouteTests/test_opencodeInterruptReturnsProviderSpecificUnsupportedAndRestoresTurnState` and `AgentControlServerChatRouteTests/test_staleManagedSessionsReturn503ForSendAndInterrupt` passed together in `/tmp/clawdmeter-opencode-interrupt-rerun.log` on 2026-06-09 at 08:19 ICT. It proves an OpenCode session no longer returns `legacy_session_retired` for Stop; it returns `501 {"error":"opencode_interrupt_not_supported"}` and locally transitions the chat store to `.interrupted` so the Send/Stop UI can recover, while preserving the existing `acp_session_not_live` behavior for stale Codex/Cursor/Gemini/Grok harness sessions.
- Final focused provider/draft pass: 4 tests passed together in `/tmp/clawdmeter-provider-draft-final-focused.log` with no filtered SwiftUI background-publish, Thread Performance Checker, SQLite vnode, or damaged-runner warnings.
- Archive-all focused pass: `AgentSessionRegistryEventStoreWireTests/test_bulkArchive_archivesAllSessionsAndReplays`, `AgentSessionRegistryEventStoreWireTests/test_bulkArchiveCoalescesSnapshotSaveWithinFeedbackBudget`, and `AgentSessionRegistryEventStoreWireTests/test_archiveReclaimEligibilityRequiresExplicitOwnedProvisioningMetadata` passed in the latest focused 7-test run. The benchmark asserts archiving 250 existing sessions stays under the 100 ms feedback budget while the replay test preserves the event-store receipt contract and the reclaim test proves managed-looking paths are not enough without explicit Continuum ownership metadata.
- Archive hover and Archive bucket UI pass: `CodeTabHoverShortcutUITests/testWorktreeHoverArchiveRevealsActionAndMovesRowToArchiveBucket` passed on 2026-06-09 at 06:17 ICT in `/tmp/clawdmeter-ui-worktree-hover-archive.log`. The run verifies hover/selection reveals one archive action, clicking persists `archivedAt`, the default sidebar drops the row, and the Archive bucket shows the archived worktree row.
- Send feedback latency pass: `SessionChatStorePendingTests/test_injectPending_surfacesVisibleSendFeedbackWithin100ms` and `SessionLauncherModelTests/test_emptyStateComposerBeginSendSurfacesFeedbackWithin100ms` passed in `/tmp/clawdmeter-unit-send-feedback-latency.log` on 2026-06-09 at 06:39 ICT. They measure 100 send attempts each and assert bound-session pending-bubble injection plus empty-state first-send `isSending` feedback stay under 100 ms.
- Latest combined touched-area pass: 25 tests passed together in `/tmp/clawdmeter-unit-code-tab-feedback-provider-firstsend-livecatalog.log` with no filtered SwiftUI background-publish, Thread Performance Checker, SQLite vnode, or damaged-runner warnings. The run covers archive speed/safety, draft tab append/isolation/first-send classification/latency, send feedback latency, all-bundled launch request routing, dynamic OpenRouter/Cursor live-catalog replacement, loaded sidebar projection latency, and shared-harness provider send/stream/error behavior. The newer focused draft/model-picker combined pass adds the rendered-picker-adjacent draft selection latency case in `/tmp/clawdmeter-unit-draft-tabs-model-picker-combined.log`, and the latest combined composer/draft matrix adds provider/model-wide draft send classification plus provider/model-wide rendered Send/Stop coverage in `/tmp/clawdmeter-composer-draft-matrix-combined.log`.
- Latest latency-gate expansion: `WorkspaceTabsTests/test_workspaceDraftTabActionsSurfaceVisibleStateWithin100ms` and `SidebarProjectionCacheTests/test_codeSidebarSearchStatusAndArchiveFeedbackUnder100ms_on500Sessions` passed in the latest combined focused run on 2026-06-09 at 06:24 ICT in `/tmp/clawdmeter-unit-code-tab-archive-tabs-launcher.log`. These assert draft tab open/select/close state changes and loaded sidebar search/status/archive projections stay under the 100 ms feedback budget.
- Workbench local-control latency expansion: `WorkbenchStateTests/test_codeWorkbenchLocalControlsSurfaceVisibleFeedbackWithin100ms` passed on 2026-06-09 at 20:21 ICT in `/tmp/clawdmeter-code-tab-unit-checks-with-workbench-latency.log`. It uses the reusable `assertCodeTabFeedbackLatency` helper to measure right-pane tab selection, review-pane visibility toggles, transcript density selection, preview-chip browser routing, and queued follow-up row insert/edit/remove, asserting each local visible-state mutation stays under the 100 ms feedback budget before async/provider work starts.
- Sidebar status-bucket state regression: `SidebarProjectionCacheTests/test_statusBucketTapStateTogglesEachBucketAndArchiveVisibility` passed with the sidebar projection suite on 2026-06-09 at 14:12 ICT in `/tmp/clawdmeter-sidebar-bucket-state.log`. It proves Active, Review, Done, and Archive bucket taps select the requested filter, tapping the selected bucket returns to All, cross-bucket taps select the newly tapped bucket, and only Archive enables archived-session visibility. The same focused run passed 14 tests, 0 failures, including the existing 500-session search/status/archive projection feedback gate under the 100 ms budget.
- Earlier failing test in this WIP: the same provider/model toggle test initially failed because nil-effort providers inherited `.max`; current run is green.
- Lint/typecheck: no root `package.json`, `Makefile`, `justfile`, or SwiftLint config was found. `apple/ClawdmeterShared/Package.swift` exists, but the primary macOS app build/test path is Xcode. Targeted Swift package checks passed `AnthropicSourceTests` in `/tmp/clawdmeter-shared-anthropic-redaction.log`, `ComposerModelPickerTests` in `/tmp/clawdmeter-shared-composer-model-picker-mainactor.log`, and the combined provider filter `AnthropicSourceTests|AntigravitySourceTests|CursorSourceTests|CodexSourceJSONLNullRateLimitsTests` in `/tmp/clawdmeter-shared-provider-redaction.log` after aligning the picker tests with `ProviderDefaultsStore`'s main-actor contract and redacting Cursor trailer diagnostics. No separate SwiftLint command has been run.
- Full suite: not run in this baseline. Current evidence is focused Code-tab regression tests plus Mac build.

## Current Code-Tab Control Inventory

Primary source of truth: `CODE_TAB_INTENT.md`, derived from the most recent 150 merged PRs touching Code-tab UI/runtime/tests. Current inventory covers:

- Navigation/titlebar: Code tab, Code filters focus, right-pane menu, `Cmd+K`, `Cmd+T`, `Shift+Cmd+T`, review-pane toggle.
- Sidebar: search, filter/group/sort, status buckets, add project, open project, open GitHub project, quick start, repo disclosure, repo `+`, repo settings, worktree row, worktree hover archive, session context menu, footer new session, external session discovery.
- Workspace/tabs/header: session tabs, close tab, tab plus menu, draft tab, terminal tab, document tab, density menu, more actions menu, rename shortcut, pop out.
- Transcript/queue/checkpoints: transcript rendering, find, jump latest, preview chip, queued follow-ups, checkpoint restore, plan halo.
- Composer: prompt field, attachments, prompt history, ANSI paste stripping, expanded editor, dictation, permission mode, model/effort, context usage, send, stop, queue follow-up, pending retry/dismiss.
- Right pane: Plan, Diff, Sources, Artifacts, Browser, PR, Terminal, tab bar, gutter buttons.
- Browser: back to chat, history nav, reload/stop, URL entry/load, run/start/stop/restart, output, comment add/cancel.

Current coverage is not exhaustive. The latest UI class covers composer targets, sidebar primary targets, repo-settings menu rows plus safe/destructive repo actions, titlebar filter focus and Cmd+K, titlebar right-pane menu rows and collapse, center-header density plus safe/destructive More-actions menu paths, center-header checkpoint create/restore behavior, dirty-tracked blocked checkpoint restore cancel behavior, untracked-overwrite blocked checkpoint restore cancel behavior, new-session and rename shortcuts, hover archive, unbounded repeated draft-tab creation via direct `+` and `Cmd+T`, rendered draft picker switching to Antigravity Gemini 3.5 Flash Thinking, rendered context-usage popover rows, draft first-prompt queue absence before send, permission-mode quick flip, full menu rows, bypass trust-sheet cancel behavior, safe shortcut paths, picker search/favorite/command-digit/effort-cycle/unsupported-effort paths, review-pane gutter/tab targets, rendered Diff/PR pane controls, rendered Sources/Artifacts pane rows plus source-click and artifact preview open/Escape-close paths, rendered Git Diff Stage/Unstage outcomes, browser controls, preview-chip routing, the workspace terminal shortcut, Terminal review-pane shortcut, rendered terminal socket-drop reconnect behavior, and build-verified prompt-history/expanded-editor rendered click tests. Source-backed sidebar status-bucket state coverage now covers Active/Review/Done/Archive selection, toggle-off, cross-bucket switching, and Archive visibility. Pending-message descriptor/store coverage now covers failed/offline retry and dismiss affordances. Prompt-history descriptor coverage now covers stable composer/sheet/row targets, saved/history filtering, empty state, and save-prompt enablement under the 100 ms budget; the new rendered prompt-history test seeds the real presentation store and is build-verified but not yet execution-proven on this host. Expanded-editor targets now compile through the UI-test target; the new rendered expanded-editor test covers save/Done/menu reuse in source and is build-verified but not yet execution-proven on this host. ANSI paste coverage now covers the stable `code.composer.paste-ansi` target, CSI/OSC/bracketed-paste stripping through `AnsiStrip.plain`, and cleaned-text insertion under the 100 ms budget. Plan halo/pane descriptor coverage now covers stable Refine/Edit/Approve targets, pending/approved/todos/empty plan states, and right-pane approval reachability. Context-usage descriptor/rendered coverage now covers context-only ring math and popover rows from supplied Code-session data. Diff descriptor/rendered coverage now covers stable toolbar/file/hunk targets and enabled/disabled state for non-destructive controls. Sources/Artifacts descriptor coverage now covers stable right-pane targets, source row capping/subtitles/counters/payload values, and artifact filename/path/preview target state without external app side effects. PR descriptor/rendered coverage now covers stable empty/menu/status/check/review targets and action gating. Deterministic PR coordinator coverage now covers approve shell success/failure/missing-gh/parse-error paths, create failure error persistence, merge success snapshot updates, and merge failure error persistence without an immediate refresh clearing the action outcome. Git diff action coverage now covers stable staged/unstaged/trash/commit targets, hunk index/worktree semantics, and rendered file-level Stage/Unstage mutations. Deterministic first-turn Send/Stop/Queue classification now covers connecting-empty provider sessions, optimistic pending first prompts, and existing streaming conversations. The known gaps are live GitHub/rerun smoke outcomes, live provider UI send/stop smoke, execution-proven rendered prompt-history/expanded-editor clicks, and full-suite/lint completion.

Superseding current note: prompt-history and expanded-editor rendered clicks are no longer gaps; both passed in `/tmp/clawdmeter-ui-composer-sheets.log`. The remaining high-value gaps are live GitHub/rerun smoke outcomes, credentialed live-provider UI send/stop smoke, and full-suite/lint completion.

## Current Provider And Model Inventory

Provider gating:

- `SessionLauncherModel` exposes static enabled providers for Claude, Codex, and Gemini/Antigravity, and adds OpenCode/OpenRouter, Cursor, and Grok only when runtime readiness checks pass.
- OpenCode readiness requires enabled provider, binary path, and non-empty auth status; its model list can be replaced by the OpenRouter live probe.
- Cursor readiness is binary-presence based; model list can be replaced by the Cursor live probe, with auth errors deferred to spawn time.
- Grok readiness is `grok` binary presence.
- The Code catalog is filtered through `ProviderEnablement.enabledProviderIDs(for: .code)`.

Bundled model paths currently present in source:

- Claude: `claude-opus-4-8-1m`, `claude-opus-4-8`, `claude-opus-4-7-1m`, `claude-opus-4-7`, `claude-opus-4-6-1m`, `claude-sonnet-4-6`, `claude-haiku-4-5-20251001`.
- Codex: `gpt-5.5`, `gpt-5.4`, `gpt-5.3-codex-spark`, `gpt-5.3-codex`, `gpt-5.2-codex`.
- Gemini/Antigravity: `gemini-3.5-flash`, `gemini-3.5-flash-thinking`, `gemini-3-pro`, `gemini-3.1-pro-high`, `gemini-3.1-pro-low`, `gemini-3-flash`, `gemini-3-flash-thinking`.
- OpenCode/OpenRouter fallback: `openai/gpt-5.5`, `anthropic/claude-opus-4.7`, `anthropic/claude-sonnet-4.6`, `google/gemini-3-pro`, `opencode-default`.
- Cursor fallback: `cursor-default`.
- Grok fallback: `grok-build`, `grok-composer-2.5-fast`.

Credential/live state is not fully verified in this baseline. The current tests prove every bundled Code provider/model/effort row is preserved in provisional launch state and launch requests, live OpenRouter/Cursor catalog replacement rows clear stale fallback model/effort selections, the shared harness path completes fake send/stream/error turns for Codex, Gemini/Antigravity, Cursor, and Grok, and the real daemon `/send` route delivers `userComposerFirstTurn` first prompts to fake live harness bridges for those same providers with `asFollowUp == false`. The real daemon scheduled-follow-up path now proves `.scheduledUserFollowUp` prompts render the user row, enter streaming state, and reach those same fake live harness bridges, while legacy synthetic scheduled prompts are blocked and stale managed scheduled sessions return `acp_session_not_live`. The real daemon `/interrupt` route now also proves fake live harness Stop dispatch for Codex, Gemini/Antigravity, Cursor, and Grok. The non-harness fake paths project Claude PTY JSONL plus OpenCode serve SSE transcript/lifecycle/error state. OpenCode now also has selected-model send-body coverage, explicit unsupported-interrupt/local Send-Stop recovery coverage, and a gated live-drive test for real `opencode serve` session creation, prompt POST, SSE reply projection, and turn completion. The no-spend run proves that live-drive case compiles and skips under the live marker gate. Rendered no-spend composer coverage proves every bundled Code provider/model renders icon-only Send when idle and icon-only Stop plus separate Queue while running. The latest direct route-class run passed `AgentControlServerChatRouteTests` 35 tests with 4 live-provider skips on 2026-06-09 at 20:21 ICT, including provider-disabled/auth-unavailable ordering, first-turn daemon send routing, scheduled follow-up routing, interrupt routing, OpenCode unsupported interrupt recovery, terminal add-on routes, Claude Code terminal WS direct-shell routing, Claude Code approve-plan route restart/progress projection, and Cursor quota usage projection. These tests do not prove live provider success or a full per-provider live UI send/stop smoke matrix.

## Current Terminal State

The Code terminal issue is fixed in current WIP and has focused green evidence:

- Terminal availability now follows local worktree eligibility instead of `supportsTerminal`. It rejects archived sessions and legacy tmux-pane/window backed sessions, while allowing paneless local worktree sessions.
- `openOrCreateWorkspaceTerminalTab` creates and selects a fresh pending direct-shell tab for every request, then asynchronously adds a terminal pane and promotes that exact pending tab to the real pane. The background promotion is serialized per session so rapid clicks cannot make registry updates overwrite sibling pane metadata.
- The shortcut/command route now registers `code.newTerminalTab` at the root command layer and posts `.newCodeTerminalTab`. The workspace route uses one shared source helper, so `Shift+Cmd+T`, the command palette, and notification posters can open a terminal even when the active foreground tab is an unsent draft, as long as an eligible same-workspace session exists.
- `WorkspaceTerminalPane` and `TerminalTabContainer` show visible pending overlays and status badges until shell output is observed, and expose contained accessibility state markers for pane-specific reconnect assertions.
- `MacTerminalView` now publishes `connecting`, `connected`, `reconnecting`, `failed`, and `disconnected` states to the SwiftUI surfaces. `WorkspaceTerminalPane` and `TerminalTabContainer` render reconnecting/failed overlays and status badges instead of leaving a dropped socket looking permanently connected or permanently starting.
- Nil-pane terminal WebSocket subscriptions now create/reuse a direct shell pane before any Claude command PTY path, so the Code Terminal surface opens a worktree shell instead of attaching to the Claude agent transport.
- `AgentControlServerChatRouteTests/test_terminalAddOnHarnessSessionCreatesDirectWorktreePane` proves the route creates a direct worktree pane for harness-style sessions. `AgentControlServerChatRouteTests/test_addDirectTerminalPaneRouteAllowsMoreThanSevenQuickTabs` proves direct terminal-pane creation no longer shares the five-second config-swap throttle or the old seven-pane ceiling. `AgentControlServerChatRouteTests/test_terminalWebSocketForClaudeCodeOpensDirectShellPane` proves a Claude Code terminal WS subscription opens a direct shell pane and does not attach a Claude PTY host.
- `TerminalPtyHostTests/test_directPtyHostStreamsInputOutputAndAppliesResize` proves direct PTY input/output and resize behavior.
- Latest focused rerun on 2026-06-09 at 04:27 ICT passed the 6-test terminal set with no visible SwiftUI background-publish warning, Thread Performance Checker priority-inversion warning, or SQLite vnode-unlinked warning in `/tmp/clawdmeter-terminal-focused-after-sendchecked.log`.
- Latest terminal UI coverage on 2026-06-09 at 16:29 ICT passed `CodeTabHoverShortcutUITests/testWorkspaceTerminalShortcutOpensDirectShellTabFromCodeTab` in `/tmp/clawdmeter-ui-terminal-current.log`: `Shift+Cmd+T` creates `code.workspace.tab.terminal`, renders `code.workspace.terminal.surface`, and reaches `Terminal connected`. The previous 12:33 ICT rerun passed three tests in `/tmp/clawdmeter-ui-terminal-reconnect-state-regressions.log`: `Shift+Cmd+T`, the tab-strip secondary Terminal action, and the right-pane `Control` + backtick shortcut all render their terminal surfaces and reach `Terminal connected` with the new reconnect-state badge wiring in place.
- Latest live socket-drop UI coverage on 2026-06-09 at 13:28 ICT passed four focused UI tests in `/tmp/clawdmeter-ui-tabs-terminal-right-pane-final.log`: repeated direct `+` appends draft tabs, repeated `Cmd+T` appends draft tabs from a selected draft, an already-rendered workspace terminal reaches `Terminal connected` -> `Terminal reconnecting` -> `Terminal connected`, and the right-pane Terminal shortcut follows the same forced WebSocket-drop recovery path.
- Earlier connected-state terminal UI coverage on 2026-06-09 at 12:27 ICT passed three tests in `/tmp/clawdmeter-ui-terminal-connected-regressions.log`: `Shift+Cmd+T`, the tab-strip secondary Terminal action, and the right-pane `Control` + backtick shortcut all render their terminal surfaces and reach `Terminal connected` instead of merely exposing a stuck `Terminal starting` state.
- Earlier UI-runner terminal shortcut coverage on 2026-06-09 at 07:36 ICT passed in `/tmp/clawdmeter-ui-code-terminal-focused.log`; `Shift+Cmd+T` created `code.workspace.tab.terminal`, rendered `code.workspace.terminal.surface`, and showed `Terminal connected`. Earlier tab-strip secondary Terminal coverage on 2026-06-09 at 11:44 ICT passed in `/tmp/clawdmeter-ui-tab-plus-terminal-context.log`; the `+` context menu row opens the same direct-shell terminal surface.
- Earlier right-pane Terminal UI coverage on 2026-06-09 at 04:49 ICT passed in `/tmp/clawdmeter-ui-review-pane-terminal-accessibility.log`; the test clicked through all review-pane tab chips, selected `code.review.tab.terminal`, and found `code.terminal.surface`. The new `Control` + backtick shortcut also opens the Terminal review pane and reaches `code.terminal.surface`.
- `WorkspaceReviewPane` now uses accessibility containment so `code.review.pane` no longer overwrites child identifiers like `code.review.tab.terminal` and `code.terminal.surface`.

## Current Workspace Tab State

The Code workspace tab cap at two was a real draft-state bug and is fixed in current WIP:

- `SessionsModel` now stores `workspaceDraftTabs: [WorkspaceDraftTab]` plus `selectedWorkspaceDraftTabId`; `draftWorkspaceTab` is the selected-draft compatibility view.
- `WorkspaceTabStrip` renders every draft tab for the active worktree, not just a single optional draft.
- The draft center-pane `+ -> Chat` action now appends another draft with inherited workspace/model defaults instead of being a no-op.
- Closing a selected draft tab now selects another draft in the same workspace when one remains, rather than dropping the center pane to the empty state.
- Draft composer stores are now cached per draft id and the model picker writes selected provider/model/effort back to the draft tab metadata, so switching between untitled tabs cannot resurrect another draft's model/text state.
- The draft composer initializer no longer publishes back into `SessionsModel` during SwiftUI view construction; draft chip persistence is now a no-op when values have not changed. This removed the UI-test stall and the `Publishing changes from within view updates` warning from the repeated-tab path.
- Empty-state and draft first sends now route through `EmptyStateFirstSendPlan`, which production code uses for spawn args and send classification. Focused unit coverage verifies every bundled Code provider/model selected on a draft targets the same worktree, preserves the selected provider/model, clears unsupported stale effort, preserves source IDs, passes the provider-prompt guard, and sends as `userComposerFirstTurn` rather than a queued follow-up.
- The sidebar selected worktree is now derived from one `activeWorkspaceKey` covering selected document tab, terminal tab, open session, and selected draft tab. This keeps an unsent draft on its owning branch and prevents stale/duplicate branch selection semantics when two worktrees are live. Focused unit evidence passed in `/tmp/clawdmeter-active-workspace-selection.log`; adjacent rendered draft/model-switch coverage passed in `/tmp/clawdmeter-ui-draft-selection-model-switch.log`.
- The repo `+` provisional path is now split so the instant feedback step can be verified without a daemon or real worktree provisioning: `createQuickSpawnProvisionalSession` reserves the city/worktree slug, inserts one provisional session keyed to that exact worktree path, clears draft/terminal/document/outside selections, and makes the new provisional worktree the active workspace. `SessionLauncherModelTests/test_quickSpawnProvisionalRowUsesReservedWorktreeAndSingleSelectionWithin100ms` passed again on 2026-06-09 at 19:04 ICT via direct `xcrun xctest`; it asserts the reserved worktree is not the previously selected stale branch, that only the new branch is selected, and that the provisional-row feedback stays under the 100 ms budget.
- Focused unit evidence: `script/run_code_tab_unit_checks.sh` passed again on 2026-06-09 at 21:31 ICT with output in `/tmp/clawdmeter-code-tab-unit-checks-composer-history-expanded.log`: `SessionLauncherModelTests` 20/20, `WorkspaceTabsTests` 36/36, `WorkbenchStateTests` 20/20, `ChatSendPerProviderTests` 15/15, and `AgentControlServerChatRouteTests` 35 executed with 4 live-provider skips and 0 failures. The WorkspaceTabs class now includes `test_sameWorkspaceModelSessionTabsAreNotCappedAtTwo`, which creates six same-worktree model sessions across Claude, Codex, Antigravity, Cursor, OpenCode, and Grok and verifies the top-tab source includes all six rather than capping at two; `test_workspaceTabStripCompactsBeyondTwoTabsOnMinimumCenterWidth`, which verifies four chat tabs plus `+` fit at the minimum center width before scrolling, the scroll region stays adjacent to tab content when only two tabs fit, and large tab counts reserve width for the always-hittable `+`; `test_composerTerminalPasteStripsAnsiAndSurfacesTextWithin100ms`, which verifies OSC/CSI/bracketed-paste cleanup appends cleaned text under 100 ms; and `test_promptHistoryPresentationFiltersStableTargetsAndSaveStateWithin100ms`, which verifies prompt-history saved/history row IDs, search filtering, empty state, and save-prompt enablement under 100 ms. Shared package coverage also passed `UIPrimitivesTests/testTextUtilitiesStripAnsiHashAndPreview` in `/tmp/clawdmeter-shared-ui-ansi-strip.log`, covering OSC title, OSC hyperlink, and bracketed-paste cleanup. The same pass also covers `test_openDraftWorkspaceTabFromSelectedDraftAppendsUnboundedSiblingDrafts`, terminal-source-from-draft routing, terminal pending feedback, repeated terminal-tab append/promotion, Claude Code terminal WS direct-shell routing, `test_approvePlanRouteForClaudeCodeMarksPlanApprovedAndRuns`, and the new Workbench local-control latency matrix. These verify draft tabs append in order from a selected session and from an already selected draft, selection is preserved, closing one draft leaves the others, per-draft composer state stays isolated, all bundled provider/model draft first-send planning stays same-worktree/first-turn with unsupported effort cleared, command/shortcut IDs route at the root layer, selected-draft terminal source resolution works, Code terminal subscriptions open worktree shells, Claude plan approval restarts in run mode with progress seeded, ANSI paste cleanup is fast and complete for terminal control sequences, prompt-history local controls are deterministic and fast, and draft/model-picker plus workbench local-state operations remain under the 100 ms feedback budget.
- Latest broad direct unit rerun: `script/run_code_tab_unit_checks.sh` passed again on 2026-06-09 at 22:15 ICT in `/tmp/clawdmeter-code-tab-unit-checks-final.log`: `SessionLauncherModelTests` 20/20, `WorkspaceTabsTests` 36/36, `WorkbenchStateTests` 20/20, `ChatSendPerProviderTests` 15/15, and `AgentControlServerChatRouteTests` 35 executed with 4 expected live-provider skips and 0 failures.
- JSONL first-message reliability: while adding the ANSI paste test, the standard runner twice exposed `ChatSendPerProviderTests/test_claudePtyJsonlStream_projectsPromptReplyUsageAndLifecycle` losing an immediately appended fake Claude user line. `JSONLTail` now arms its vnode source before the catch-up drain so appends cannot land between catch-up reading and event registration. The same standard runner passed after this fix in `/tmp/clawdmeter-code-tab-unit-checks-ansi-paste-after-tail-source-cleanup.log`.
- Focused first-send evidence: `WorkspaceTabsTests/test_workspaceDraftFirstSendPlanUsesSelectedDraftModelAsFirstTurn` passed in `/tmp/clawdmeter-unit-code-tab-archive-tabs-launcher-provider-firstsend.log`; `WorkspaceTabsTests/test_workspaceDraftFirstSendPlanCoversEveryBundledCodeProviderModel` passed again in `/tmp/clawdmeter-composer-draft-matrix-combined.log`. The latest direct `xcrun xctest -XCTest ClawdmeterMacTests.SessionLauncherModelTests` run on 2026-06-09 at 19:04 ICT passed 20 tests, including connecting-empty first-turn Send/Stop/Queue classification, optimistic pending first prompts, all-bundled provisional launch request propagation, dynamic OpenRouter/Cursor live-catalog replacement, 200 rapid provisional provider/model toggles under the 100 ms budget, and the repo `+` stale-branch/single-selection regression. The same pass also keeps the provider tests isolated from the app's default-off provider flags.
- Focused UI evidence: `CodeTabHoverShortcutUITests/testWorkspaceNewTabButtonCreatesUnboundedChatDraftTabsFromCodeTab` and `CodeTabHoverShortcutUITests/testWorkspaceNewTabShortcutCreatesUnboundedChatDraftTabsFromDraftSelection` passed again in `/tmp/clawdmeter-workspace-tabs-unbounded-ui-current.log`, and earlier alongside both terminal reconnect tests in `/tmp/clawdmeter-ui-tabs-terminal-right-pane-final.log`. The latest same-worktree rendered session-tab rerun passed in `/tmp/clawdmeter-ui-same-worktree-session-tabs.log`, proving six provider/model sessions under one worktree render as tabs and crowded-tab switching updates the center header. The latest rendered first-prompt rerun passed in `/tmp/clawdmeter-ui-first-prompt-after-active-turn-fix.log`. Earlier focused runs also passed in `/tmp/clawdmeter-ui-workspace-tabs-unbounded.log`, `/tmp/clawdmeter-ui-workspace-tabs-shortcut-unbounded.log`, `/tmp/clawdmeter-ui-first-prompt-not-queued.log`, `/tmp/clawdmeter-ui-permission-mode-chip-rerun.log`, `/tmp/clawdmeter-ui-tabs-effort-final.log`, and `/tmp/clawdmeter-ui-tabs-picker-combined.log`. They verify repeated direct `+` clicks append five draft tabs from the primary Code tab, repeated `Cmd+T` appends from a selected draft, the first typed draft prompt enables the bottom-right icon-only send target without exposing queued follow-ups before send, the permission chip quick-flips and accepts `Cmd+Shift+1/2`, the tab strip reports the rendered session-plus-drafts count, the rendered picker can select the Antigravity rail row `gemini-3.5-flash-thinking`, the draft chip updates to Gemini 3.5 Flash, the active workspace tab stays on the draft, `Cmd+1` in the picker does not navigate to Chat, picker search/favorites work, `Cmd+Opt+E` and `Cmd+Opt+Shift+E` update the visible effort chip, and unsupported-effort Claude Haiku clears stale effort. The current UI test now also asserts the `+` control remains hittable after five drafts and can append a sixth, matching the scroll-region source fix.
- Latest local tab-cap rerun on 2026-06-09 at 18:13 ICT passed `CodeTabHoverShortcutUITests/testWorkspaceNewTabButtonCreatesUnboundedChatDraftTabsFromCodeTab` and `CodeTabHoverShortcutUITests/testWorkspaceNewTabShortcutCreatesUnboundedChatDraftTabsFromDraftSelection` with `xcodebuild test -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac UI Tests)" -destination 'platform=macOS'`. The direct `+` case rendered five draft tabs and selected the fifth; the `Cmd+T` case kept appending while a draft tab was already selected.
- Terminal tabs now follow the same unbounded workspace-tab contract: repeated terminal requests append distinct pending tabs immediately, do not reuse the first pending direct-shell tab, and promote all pending tabs to direct-shell panes. Focused unit/route evidence opens eight workspace terminal tabs and eight daemon terminal panes in `/tmp/clawdmeter-terminal-tabs-unbounded.log`.

Residual terminal follow-ups:

- Broader close/dedupe/accessibility coverage is still useful around crowded terminal and document tab rows.
- The UI-runner terminal shortcut, tab-strip Terminal action, and right-pane Terminal shortcut paths are green after the earlier damaged-runner workaround, and now require `Terminal connected` rather than accepting a perpetual starting state.

## Current Responsiveness Symptoms

Green evidence:

- Terminal pending-tab feedback test passes within the 100 ms budget.
- Provisional provider/model launch configuration test passes and verifies final spawn uses the latest selected provider/model/effort.
- Dynamic OpenRouter/Cursor live-catalog replacement test passes and verifies stale fallback picker selections normalize to live rows with unsupported effort cleared.
- Draft first-send classification tests pass and verify switched draft sends stay first-turn and same-worktree after model/provider toggles across every bundled Code provider/model.
- Send feedback latency tests pass and verify both empty-state first-send `isSending` state and bound-session pending-bubble injection surface within the 100 ms budget.
- Composer Send/Stop rendered no-spend coverage passes and verifies the shared action descriptor stays icon-only for every bundled Code provider/model, puts Stop in place of Send while running, keeps Queue separate, and emits nonblank distinct idle/running composer snapshots.
- Per-provider deterministic send coverage passed 15 tests in `/tmp/clawdmeter-chat-send-per-provider-after-active-turn-fix.log`, covering harness routing/delivery/error projection for Codex, Gemini/Antigravity, Cursor, and Grok plus Claude PTY JSONL and OpenCode SSE prompt/reply/lifecycle projection without live provider quota.
- Draft tab open/select/close feedback now has a 100 ms unit gate over 50 draft opens, 50 selections, and 25 closes.
- Draft model-picker selection feedback now has a 100 ms unit gate over 49 draft selections and focused UI coverage proving the rendered picker updates a draft to Antigravity Gemini 3.5 Flash Thinking instead of opening a stale session, while picker `Cmd+1`, search, favorite, effort menu, and unsupported-effort clearing all stay on the active draft.
- Loaded sidebar search/status/archive projection feedback now has a 100 ms unit gate on the deterministic 500-session fixture.
- Titlebar filter focus, Cmd+K search focus, repo-settings menu rows plus safe/destructive repo actions, titlebar right-pane menu rows/collapse, center-header density plus safe/destructive More-actions menu paths, center-header checkpoint create/restore behavior, hover archive, repeated draft-tab append paths, rendered context-usage popover rows, and draft first-prompt queue absence now have focused UI coverage.
- Rendered right-pane Diff/PR controls now have focused UI coverage for seeded dirty-diff controls and loaded PR action-menu paths.
- PR coordinator action coverage passed 18 focused tests on 2026-06-09 at 17:47 ICT with `xcodebuild test -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac)" -destination 'platform=macOS' -only-testing:ClawdmeterMacTests/PRCoordinatorTests`. This covers stable PR descriptors plus approve shell success/failure/missing-gh/parse-error paths, Create PR failure error persistence, Merge success snapshot updates, and Merge failure error persistence.
- Sources/Artifacts right-pane descriptor coverage passed 2 focused tests on 2026-06-09 at 17:55 ICT with `xcodebuild test -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac)" -destination 'platform=macOS' -only-testing:ClawdmeterMacTests/ReviewPaneContentDescriptorTests`. This covers stable Sources/Artifacts accessibility targets, source row capping/subtitles/counters/payload values, and artifact filename/path/preview targets.
- Sources/Artifacts rendered pane coverage passed on 2026-06-09 at 18:11 ICT with `CodeTabHoverShortcutUITests/testSourcesAndArtifactsReviewPanesRenderSeededTranscriptContentAndClicks`. It seeds `Read`, `WebFetch`, and `Write` transcript/tool events, verifies the rendered `notes.md`, `https://example.com/continuum-docs`, and `report.md` rows, clicks a source row through the in-app open handler without launching external apps under UI tests, opens artifact preview state, and closes that preview state with Escape.
- Prior Code-tab release audit records 200 provisional picker flips under the sub-100 ms per-toggle budget.
- `AgentSessionRegistry` event-log replay no longer blocks `init` on the MainActor. Replay now runs asynchronously, merges with live sessions that may appear before replay finishes, and `AgentSessionRegistryEventStoreWireTests/test_replaySeedDoesNotBlockRegistryInitOnMainActor` verifies construction stays under 100 ms with a replayable 750-event log.
- Code-tab hover/shortcut UI suite passed end-to-end through the Mac UI runner: 7 tests, 0 failures, 220.750 seconds elapsed. The long elapsed time is test-environment overhead; the log repeatedly shows an external VLC window treated as a UI interruption.

Warning history and remaining performance risks:

- Earlier focused logs showed repeated SwiftUI background-thread publication warnings for `AgentControlClient`, `ChatV2Store`, `ComposerSendController`, `ChatCursorUsageSource`, `MemoizedDerivedStore<Array<AgentSession>, Array<SidebarRow>>`, and `ProviderDefaultsStore`.
- Current WIP isolates `AgentControlClient.sendChecked` status publication, `ProviderDefaultsStore`, and the AppDelegate menu-bar repaint path on the main actor. Latest terminal and replay focused logs no longer show SwiftUI background-publish or `ComposerSendController.text` binding warnings. `ComposerModelPickerTests` now run on the main actor and pass in `/tmp/clawdmeter-shared-composer-model-picker-mainactor.log`, keeping the shared model-picker tests aligned with that isolation.
- The latest offscreen `ImageRenderer` composer-matrix run passed but emitted expected render-harness warnings for `FocusState` and flattened AppKit representables from the composer `TextField`/drop destination. It also surfaced existing Swift 6 sendability warnings in unrelated terminal/chat-route tests during the build phase; no composer-matrix assertion failed.
- Latest full Code-tab hover/shortcut UI log also shows no SwiftUI background-publish, Thread Performance Checker, SQLite vnode, or damaged-runner warning in `/tmp/clawdmeter-ui-code-tab-hover-class.log`.
- Latest focused terminal reconnect-state logs show no SwiftUI background-publish, Thread Performance Checker, SQLite vnode, or damaged-runner warning in `/tmp/clawdmeter-terminal-reconnect-state-unit.log`, `/tmp/clawdmeter-ui-terminal-reconnect-state-regressions.log`, `/tmp/clawdmeter-ui-tabs-terminal-socket-drop-final.log`, or `/tmp/clawdmeter-ui-tabs-terminal-right-pane-final.log`.
- Earlier focused right-pane Terminal UI log shows no SwiftUI background-publish, Thread Performance Checker, SQLite vnode, or damaged-runner warning in `/tmp/clawdmeter-ui-review-pane-terminal-accessibility.log`. It did record an external Chrome window interruption, but the suite recovered and both focused tests passed.
- XCTest host startup still boots normal app runtime pieces, usage pollers, App Intents metadata, KVS, and provider services, which adds noise and can hide Code-tab-specific hangs.
- External desktop state can still slow UI runs: `/tmp/clawdmeter-ui-code-tab-hover-class.log` repeatedly recorded `org.videolan.vlc` as an interruption window, although the suite recovered and passed.

Remaining release-critical work for the macOS responsiveness requirement is broader UI coverage and live interaction measurement across every Code-tab control, especially deeper menus, right-pane actions, and live provider send/stop paths.

## Crashes, Blocks, And Console Errors

- UI-test runner block: macOS previously reported `ClawdmeterMacUITests-Runner` as damaged. Removing quarantine/provenance attrs and ad-hoc signing the generated runner unblocked XCUITest.
- Current host-runner block: after the quick-spawn provisional test was added, `xcodebuild build-for-testing -project apple/Clawdmeter.xcodeproj -scheme "Clawdmeter (Mac)" -destination 'platform=macOS'` succeeded, but focused `xcodebuild test` invocations for both the new `SessionLauncherModelTests/test_quickSpawnProvisionalRowUsesReservedWorktreeAndSingleSelectionWithin100ms` and a previously green `WorkspaceTabsTests/test_openDraftWorkspaceTabAllowsMultipleUntitledTabsPerWorkspace` stalled before XCTest printed any test-case output. One run launched the app host and detached it under `launchd`; another remained in `xcodebuild` before launching the app. Direct `xcrun xctest` initially could not load the hosted bundle because `Continuum.debug.dylib` and `Sparkle.framework` are resolved through app-host rpaths; the `ClawdmeterMacTests` target now carries `@loader_path/../../../../MacOS` and `@loader_path/../../../../Frameworks`, so the direct fallback works after a normal `build-for-testing` without mutating DerivedData. The reproducible fallback is `script/run_code_tab_unit_checks.sh`, which performs the build-for-testing step, locates the app-hosted test bundle, and runs the focused direct XCTest classes. The direct route exposed and fixed a real legacy-model-route ordering bug plus provider-enable test isolation gaps. Treat the remaining block as local `xcodebuild test` orchestration, not as product-test failure.
- KVS entitlement warning: test host logs `Unable to find entitlement for KVS store` and `Trying to initialize NSUbiquitousKeyValueStore without a store identifier`.
- SQLite warning: previously selected tests emitted `database integrity compromised by API violation: vnode unlinked while in use` for temporary orchestration sqlite/WAL/SHM paths. Current WIP adds explicit event-store close in test teardown, and the latest focused terminal rerun did not emit the warning.
- App Intents/linkd noise is present during test-host startup in prior runs.
- No current focused terminal/model-toggle XCTest failure after the latest run.

## Safety And Data-Loss Baseline

High-risk surfaces identified for the Code tab:

- Worktree provisioning and cleanup: quick spawn creates a provisional session, provisions a new worktree, records ownership metadata, and later cleanup can delete/reclaim owned worktrees. It must never operate on user-owned paths without `ownsWorktree` and metadata checks.
- Archive all/archive worktree: current WIP coalesces registry mutations for speed and schedules cleanup after rows disappear. Focused tests now cover bulk archive replay, 250-session archive feedback under 100 ms, and worktree reclaim eligibility. Reclaim now requires a code session with `ownsWorktree`, matching `WorktreeProvisioningMetadata.worktreePath`, and a non-empty ownership marker; path shape alone is not enough to teardown or trash a worktree.
- Add project/clone/quick start: can create folders, clone repos, or register existing folders. These need visible progress, cancellation, error states, and no hidden scans without opt-in.
- Setup script execution: repo setup scripts run during provisioning and must be clearly user-initiated, visible, cancellable where possible, and must not run silently on old sessions.
- Provider routing: model/provider changes must not send code, secrets, logs, or prompts to a stale provider. The current all-bundled launch-configuration test covers provisional session routing for the bundled catalog; live-catalog replacement coverage prevents stale OpenRouter/Cursor fallback models from surviving into picker defaults; draft first-send planning covers every bundled provider/model for same-worktree routing, selected provider/model preservation, unsupported-effort clearing, first-turn classification, and prompt-guard acceptance; rendered UI coverage verifies a draft picker switch to Antigravity Gemini 3.5 Flash Thinking stays on the draft rather than opening a stale session; shared-harness fake matrix covers Codex, Gemini/Antigravity, Cursor, and Grok prompt delivery plus completion/error projection; daemon route coverage now verifies `userComposerFirstTurn` with `asFollowUp == false` and `.scheduledUserFollowUp` prompts reach fake live harness bridges for Codex, Gemini/Antigravity, Cursor, and Grok; stale managed scheduled sessions return `acp_session_not_live` without legacy fallback; daemon interrupt coverage verifies Stop dispatch reaches the live harness cancel path for those providers; rendered no-spend composer coverage verifies the shared icon-only Send/Stop surface for every bundled provider/model; and non-harness fake coverage covers Claude PTY JSONL plus OpenCode serve SSE transcript/lifecycle/error projection. Live provider paths and the per-provider live UI send/stop matrix are still pending.
- Credentials/logs: provider tokens, prompt bodies, provider response bodies, loopback tokens, and relay response bodies must remain out of logs and analytics. Current source has local loopback token usage and provider enablement gating. Current WIP removes credential-derived token length/fingerprint/prefix logging from Anthropic usage polling, Claude token auto-import, Watch token receive, live-activity APNS pusher, APNS gateway, and APNS device-token store paths. It also removes public provider body previews from Anthropic, Antigravity/Gemini, and Codex usage parsing, redacts Cursor trailer diagnostics to byte counts, redacts relay grant error bodies to byte counts, removes `ask_user` prompt text from SidecarAsk logs, and stops public logging of git/whois stderr. Focused redaction guards passed in `/tmp/clawdmeter-credential-log-redaction.log` and `/tmp/clawdmeter-public-diagnostics-redaction.log`; a complete whole-app log-redaction audit is still pending.

## Assumptions And Gaps

- This audit is current-state after WIP, not a pristine starting point.
- Focused tests and Mac build are green; full test suite and full UI suite were not rerun in this baseline.
- Instruments/Time Profiler/Hangs/Main Thread Checker have not been run yet beyond XCTest's visible runtime warnings.
- Provider credentials and live smoke tests were not checked for every configured provider/model.
- `docs/CODE_TAB_PROVIDER_MATRIX.md` now exists with source-level capability, credential, mock coverage, and live-smoke status. The latest non-spend local readiness probe found binaries for Claude, Codex via Hermes, Antigravity `agy`, OpenCode, Cursor Agent, and Grok, but account validity and live prompt success remain unrun.
- No-spend XCTest app bootstrap now defers provider side effects and real user-data reads. The first gated `LiveDriveTests` compile/skip run exposed usage pollers and Claude token auto-import under ordinary XCTest; later passes also exposed shared mobile-command audit replay and test chat-cwd/Claude trust writes reaching user support files. Current WIP gates Claude auto-import, provider pollers, provider runtime warmers, and mobile-command audit replay behind `CLAWDMETER_LIVE_VERIFY=1`, `CLAWDMETER_ALLOW_PROVIDER_SPEND=1`, and `~/.continuum-live-verify`, or explicit app-test support dirs. Current rerun evidence shows those side effects deferred, AgentControlServer fixtures using `MobileCommandOutbox(replaysAuditLogOnStart: false)`, session/workspace stores isolated to temp test support (`No sessions.json`), and Claude PTY chat-cwd/trust writes isolated under temp roots. The focused source-policy test verifies the guard.
- The goal's full 100 ms bound is not yet proven for every Code-tab element; current gates cover the highest-risk model-toggle, terminal, archive, draft-tab, and loaded-sidebar projection paths.
